# DECISIONS.md

주요 기술 결정, 가정, AI 제안 채택/기각, 못 끝낸 기능. 각 항목은 해당 커밋과 대응된다.

## 주요 결정

### 1. 프레임워크 — Vite + React + TypeScript
- 결정: Vite + React 19 + TS (create-vite `react-ts` 템플릿)
- 이유: 과제는 단일 화면 SPA이고 SSR이 필요 없다. Next.js를 쓰면 mock API와 localStorage persist가
  서버 렌더링과 충돌해 hydration 처리가 늘어난다. 실작업 6~10시간 상정에서 셋업 비용이 가장 낮다.
- 대안과 기각 이유: Next.js App Router — 과제가 허용하지만 위 이유로 이번 범위에선 이점이 없다.
- 관련: init

### 2. 상태 관리 — React state + custom hook
- 결정: 외부 상태 라이브러리 없이 `useState`/`useReducer` + 커스텀 훅으로 보드 상태를 관리한다.
- 이유: 상태 규모가 작고(지원자 목록 + UI 상태), 낙관적 업데이트·롤백·경쟁 상태 처리 로직을
  훅 안에 직접 드러내는 것이 "핵심 상태 로직을 라이브러리에 위탁하지 않았다"는 점을 보여주기에 유리하다.
- 대안과 기각 이유:
  - Zustand — 보일러플레이트는 적지만 롤백은 여전히 직접 구현해야 하므로 얻는 것이 적다.
  - Redux Toolkit — 이 규모에서 보일러플레이트 과다.
  - TanStack Query optimistic mutation — 낙관적 업데이트·롤백을 라이브러리가 대신한다. 과제가
    "핵심 상태 로직을 통째로 대신하는 라이브러리"에 이유 문서화를 요구하고, 직접 구현 여부가 평가될 수 있어 제외.
- 관련: init (optimistic-update 단계에서 구체 전략 추가 예정)

### 3. 스타일링 — CSS Modules
- 결정: CSS Modules (Vite 기본 지원)
- 이유: 의존성 0, 컴포넌트 단위 스코프, 포커스 링 등 접근성 스타일을 직접 제어하기 쉽다. UI 디자인 배점이 낮아 도구 투자 가치가 작다.
- 대안과 기각 이유: Tailwind — 설정 추가 및 긴 클래스로 가독성 저하 우려. styled-components — 런타임 CSS-in-JS로 1,000건 렌더 시 부담.
- 관련: init

### 4. 단계 이동 방식 — 명시적 액션 버튼
- 결정: 카드에 단계 이동 버튼/선택 UI를 두는 방식. 드래그앤드롭은 채택하지 않음.
- 이유: 과제가 DnD 또는 액션 버튼 택1을 허용한다. 버튼은 Must를 충족하면서 키보드 접근성이 기본으로 확보되어
  a11y-keyboard 단계의 별도 대체 경로 구현이 필요 없다. 절약한 시간을 롤백·경쟁 상태 처리에 쓴다.
- 대안과 기각 이유: @dnd-kit — UX는 좋지만 키보드 대체 경로 필수, virtualization과 충돌 가능, 약 1~1.5시간 추가.
- 관련: init (stage-move 단계에서 구체 UI 결정)

### 5. 폴더 구조 — 기능별 features/ + 공유 shared/
- 결정: `src/features/<기능>/`에 컴포넌트와 CSS를 함께 두고, 단계 정의·타입·유틸 등 여러 기능이 쓰는 것만 `src/shared/`에 둔다. mock API는 `src/api/`.
- 이유: 과제가 기능 단위 커밋을 요구하므로 폴더 경계와 커밋 경계를 일치시키면 히스토리에서 변경 범위가 바로 보인다.
- 대안과 기각 이유: 종류별 `components/ hooks/ types/` — 파일이 늘면 어떤 파일이 어떤 기능에 속하는지 추적이 어렵다.
- 관련: board-layout

## 가정 (요구사항이 모호한 지점)

### 가정: mock API는 자체 구현, 초기 데이터는 직접 생성
- 원문: 1장 "데이터는 제공되는 mock API(또는 로컬 mock 서버)를 통해 읽고 씁니다", 4장 "mock API — 자체 구현"
- 가정: 제공되는 mock API·데이터 파일은 없는 것으로 보고 자체 구현한다. 지원자 시드 데이터는 고정 seed 기반 생성기로 1,000건을 만든다.
- 근거: 원문 7장을 확인하지 못했고, 4장이 명시적으로 자체 구현을 허용한다.
- 영향: mock-api

### 가정: 지원자 상세 필드
- 원문: 카드에는 "이름·직무·지원일·현재 단계"만 명시. 상세 보기의 내용은 미명시.
- 가정: 상세 패널에는 위 4개 + 이메일·연락처·경력연수·메모를 표시한다.
- 근거: 채용 담당자가 상세에서 볼 법한 최소 정보. 시드 생성기에서 만들기 쉽다.
- 영향: mock-api(데이터 모델), detail-panel

### 가정: 불합격은 5번째 독립 컬럼
- 원문: "서류검토 → 면접 → 처우협의 → 최종합격 / 불합격"
- 가정: 불합격을 최종합격과 나란한 5번째 컬럼으로 두고, 어느 단계에서든 불합격으로 보낼 수 있게 한다.
- 근거: 실제 채용에서 불합격은 특정 단계 다음이 아니라 모든 단계에서 발생하는 종착 상태다. 컬럼과 단계를 1:1로 두면 상태 모델이 단순해진다.
- 영향: board-layout, stage-move (구체적 이동 규칙은 stage-move에서 확정)

### 가정: 제출은 공개 GitHub 레포 링크
- 원문: 제출 형식 미확인(7장 없음).
- 가정: 공개 레포 `gygy7151/recruitment-pipeline-board`의 링크와 커밋 히스토리로 제출한다.
- 영향: init (레포 public)

## AI 제안 채택/기각

### 기각: 가로 스크롤 오른쪽 여백 우회 코드
- 제안: 자가 리뷰에서 "WebKit/Blink는 가로 스크롤 flex 컨테이너의 오른쪽 패딩을 스크롤 영역에 포함하지 않는다"고 지적하고, CSS 변수 2개와 `::after` 스페이서로 우회하는 코드를 넣었다. (출처: board-layout 리뷰 1번)
- 결정: 채택하지 않음. 원래의 `padding: 16px 24px 24px`로 되돌림.
- 이유: 추측을 기록할 수 없어 Chrome DevTools Protocol로 실제 측정했더니 **Chrome 154에서 재현되지 않았다.** 두 방식 모두 스크롤 끝 여백이 24px로 동일하다. 재현되지 않는 버그의 우회 코드는 불필요한 복잡도다. (측정 표는 PROMPTS.md [board-layout] 참조)
- 남은 한계: Safari(WebKit)는 헤드리스 자동화에 사용자 설정 변경이 필요해 확인하지 못했다.

### 이월: 컬럼 본문의 키보드 스크롤
- 제안: 컬럼 본문이 `overflow-y: auto`인데 포커스를 받지 못해 키보드만으로 스크롤할 수 없다 (WCAG 2.1.1). (출처: board-layout 리뷰 4번)
- 결정: a11y-keyboard 단계로 이월.
- 이유: 카드가 포커스 가능해지면 포커스 이동으로 스크롤되어 저절로 해결될 수 있다. 지금 `tabindex="0"`을 주면 빈 컬럼까지 탭 순회에 들어가 탭 이동만 길어진다. 카드를 올린 뒤 실측해서 판단한다.

### 채택: 미사용 타입·CSS 중복·뷰포트 단위
- 제안: 미사용 `StageId` 타입 제거, `flex: 0 0 280px`와 중복된 `min-width` 제거, 구식 `clip` → `clip-path`, `100vh` → `100dvh`. (출처: board-layout 리뷰 2·3·5번)
- 결정: 모두 채택.
- 이유: 미사용 코드 방치는 과제가 명시한 감점 신호다. 나머지는 동작 변화 없이 일관성만 올린다. `StageId`는 mock-api 단계에서 실제로 쓸 때 다시 추가한다.

## 못 끝낸 기능

(wrap-up에서 작성)
