# DECISIONS.md

주요 기술 결정, 가정, AI 제안 채택/기각, 못 끝낸 기능. 각 항목은 해당 커밋과 대응된다.

## 주요 결정

### 1. 프레임워크 — Vite + React + TypeScript
- 결정: Vite + React 19 + TS (create-vite `react-ts` 템플릿)
- 이유: 과제는 단일 화면 SPA이고 SSR이 필요 없다. Next.js를 쓰면 mock API와 localStorage persist가
  서버 렌더링과 충돌해 hydration 처리가 늘어난다. 실작업 6~10시간 상정에서 셋업 비용이 가장 낮다.
- 대안과 기각 이유: Next.js App Router — 과제가 허용하지만 위 이유로 이번 범위에선 이점이 없다.
- 관련: init

### 2. 상태 관리 — React state + custom hook
- 결정: 외부 상태 라이브러리 없이 `useState`/`useReducer` + 커스텀 훅으로 보드 상태를 관리한다.
- 이유: 상태 규모가 작고(지원자 목록 + UI 상태), 낙관적 업데이트·롤백·경쟁 상태 처리 로직을
  훅 안에 직접 드러내는 것이 "핵심 상태 로직을 라이브러리에 위탁하지 않았다"는 점을 보여주기에 유리하다.
- 대안과 기각 이유:
  - Zustand — 보일러플레이트는 적지만 롤백은 여전히 직접 구현해야 하므로 얻는 것이 적다.
  - Redux Toolkit — 이 규모에서 보일러플레이트 과다.
  - TanStack Query optimistic mutation — 낙관적 업데이트·롤백을 라이브러리가 대신한다. 과제가
    "핵심 상태 로직을 통째로 대신하는 라이브러리"에 이유 문서화를 요구하고, 직접 구현 여부가 평가될 수 있어 제외.
- 관련: init (optimistic-update 단계에서 구체 전략 추가 예정)

### 3. 스타일링 — CSS Modules
- 결정: CSS Modules (Vite 기본 지원)
- 이유: 의존성 0, 컴포넌트 단위 스코프, 포커스 링 등 접근성 스타일을 직접 제어하기 쉽다. UI 디자인 배점이 낮아 도구 투자 가치가 작다.
- 대안과 기각 이유: Tailwind — 설정 추가 및 긴 클래스로 가독성 저하 우려. styled-components — 런타임 CSS-in-JS로 1,000건 렌더 시 부담.
- 관련: init

### 4. 단계 이동 방식 — 명시적 액션 버튼
- 결정: 카드에 단계 이동 버튼/선택 UI를 두는 방식. 드래그앤드롭은 채택하지 않음.
- 이유: 과제가 DnD 또는 액션 버튼 택1을 허용한다. 버튼은 Must를 충족하면서 키보드 접근성이 기본으로 확보되어
  a11y-keyboard 단계의 별도 대체 경로 구현이 필요 없다. 절약한 시간을 롤백·경쟁 상태 처리에 쓴다.
- 대안과 기각 이유: @dnd-kit — UX는 좋지만 키보드 대체 경로 필수, virtualization과 충돌 가능, 약 1~1.5시간 추가.
- 관련: init (stage-move 단계에서 구체 UI 결정)

### 5. 폴더 구조 — 기능별 features/ + 공유 shared/
- 결정: `src/features/<기능>/`에 컴포넌트와 CSS를 함께 두고, 단계 정의·타입·유틸 등 여러 기능이 쓰는 것만 `src/shared/`에 둔다. mock API는 `src/api/`.
- 이유: 과제가 기능 단위 커밋을 요구하므로 폴더 경계와 커밋 경계를 일치시키면 히스토리에서 변경 범위가 바로 보인다.
- 대안과 기각 이유: 종류별 `components/ hooks/ types/` — 파일이 늘면 어떤 파일이 어떤 기능에 속하는지 추적이 어렵다.
- 관련: board-layout

### 6. Mock API 저장 모델 — 모듈 메모리를 서버로, 쓰기 성공 시에만 localStorage
- 결정: 모듈 로드 시 localStorage에서 한 번 복원해 모듈 변수에 두고, 쓰기가 성공했을 때만 직렬화해 저장한다. 매 호출마다 읽고 쓰지 않는다.
- 이유: 1,000건 JSON을 호출마다 직렬화하지 않아 비용이 낮다. 더 중요한 건 이 구조가 **실제 서버처럼 동작한다**는 점이다. 같은 카드에 이동 요청이 연달아 들어오면 나중에 도착한 응답이 이기는 상황이 그대로 재현되어, 경쟁 상태 처리(#6)를 클라이언트가 실제로 풀어야 한다. 서버가 버전 필드로 미리 막아버리면 도전 요구사항을 우회하는 셈이 된다.
- 대안과 기각 이유: 매 호출마다 localStorage 읽고 쓰기 — 상태가 한 곳에만 있어 단순하지만 느리고, 병행 요청에서 읽기-쓰기 경합이 깨진다.
- 관련: mock-api, optimistic-update(#6)

### 7. 낙관적 업데이트 롤백 전략 — 카드별 서버 확정 상태
- 결정: 카드마다 "서버가 마지막으로 확정해 준 모습"을 들고 있다가, 이동이 실패하면 **그 카드만** 그 단계로 되돌린다. 이동 직전 전체 목록을 저장해 두는 방식은 쓰지 않는다.
- 이유: 전체 스냅샷을 되돌리면 그 사이 다른 카드가 옮겨간 것까지 함께 지워진다. 같은 카드를 두 번 옮겼을 때는 첫 번째 성공까지 없던 일이 된다. 카드별 확정 상태를 기준으로 하면 실패한 카드만 정확한 지점으로 돌아간다.
- 세부: 성공 응답은 그 요청이 최신이 아니어도 확정 상태를 갱신한다. 서버가 실제로 썼기 때문이다. 이것을 빠뜨리면 "첫 요청 성공 + 두 번째 요청 실패"에서 롤백 목적지가 틀린다.
- 대안과 기각 이유: 이동 직전 전체 목록 snapshot — 코드는 짧지만 위 두 경우에서 틀린다.
- 관련: optimistic-update

### 8. 경쟁 상태 처리 — 카드별 요청 직렬화 + 요청 시퀀스
- 결정: 같은 카드의 네트워크 호출을 줄로 엮어 앞 호출이 끝난 뒤 다음을 보낸다. 여기에 카드별 요청 시퀀스를 더해 늦게 온 응답은 화면에 반영하지 않는다. 화면은 줄을 서지 않고 클릭 즉시 바뀐다.
- 이유: mock API가 호출마다 200~800ms 난수 지연을 주므로 **나중에 보낸 요청이 먼저 도착할 수 있다.** 그러면 서버의 최종 쓰기는 마지막에 도착한 요청이 정하는데 화면은 마지막에 보낸 요청을 따라 둘이 갈린다. 추측이 아니라 실제로 재현했다. 같은 카드를 30ms 간격으로 두 번 옮긴 실험에서 **시퀀스만 있을 때 12회 중 7회가 불일치했고, 직렬화를 넣자 0회가 되었다.**
- 두 장치의 역할이 다르다. 직렬화는 서버 쓰기 순서를 사용자 의도 순서와 맞춘다. 시퀀스는 화면이 사용자의 마지막 의도를 지키게 한다.
- 대안과 기각 이유: 응답 후 서버 값으로 화면을 강제 동기화 — 불일치는 없어지지만 사용자가 마지막으로 누른 결과가 되돌려져 "늦게 도착한 응답이 최신 상태를 덮어쓰지 않음" 기준과 충돌한다. 카드별 잠금(버튼 비활성) — 경쟁을 막지만 낙관적 반영 뒤에도 못 누르게 하는 것이라 요구사항 회피로 보인다.
- 관련: optimistic-update

### 9. 저장되는 단계와 화면 컬럼을 분리
- 결정: `STAGES`(저장되는 5단계)는 그대로 두고 `COLUMNS`(화면의 4컬럼)를 따로 정의한다. 결과 컬럼 하나가 `hired`·`rejected` 두 단계를 담는다. 카드 이동은 단계가 아니라 **컬럼 단위**로 한 칸씩 움직인다.
- 이유: 컬럼 구성은 화면의 문제이고 단계는 데이터의 문제다. 분리해 두면 mock API·시드·이미 저장된 데이터를 전혀 건드리지 않고 컬럼 구성을 바꿀 수 있다. 실제로 이 변경에서 `src/api` 아래 파일은 한 줄도 바뀌지 않았다.
- 대안과 기각 이유: `result` 단계 + 별도 `outcome` 필드로 모델을 바꾸는 방법 — "컬럼 = 단계" 1:1은 유지되지만 Applicant 모델·시드·저장소 마이그레이션이 모두 필요하고, 모델에 차원이 하나 늘어난다.
- 안전장치: `columnIdOf`는 어느 컬럼에도 매핑되지 않은 단계를 마지막 컬럼으로 보낸다. 그냥 버리면 그 카드가 화면에서 아무 표시 없이 사라진다. card-list에서 같은 문제를 한 번 겪었다.
- 관련: stage-merge(#14)

### 10. mock API가 단계 변경의 자리 규칙을 받는다
- 결정: `moveStage(id, toStage, placement)`에서 `placement`가 `'end'`(목록 끝으로) 또는 `'keep'`(자리 유지)을 받는다. 기본값은 두지 않아 호출할 때마다 고르게 한다.
- 이유: 자리 규칙이 화면에만 있으면 새로고침했을 때 카드가 다른 자리에 나타난다. 화면과 서버가 갈리는 것은 #6에서 실측으로 잡아낸 문제라(같은 카드 연속 이동에서 12회 중 7회 불일치) 다시 만들 수 없다. 저장 쪽에도 같은 구분이 있어야 한다.
- 완료된 기능(#3)의 표면을 넓히는 변경이지만 지연 200~800ms와 실패율 15%는 그대로다. 요구사항 표면은 건드리지 않았다.
- 기본값을 두지 않은 이유: 새 호출부가 생길 때 자리를 반드시 고르게 한다. 조용히 `'end'`가 적용돼 카드가 튀는 사고를 막는다.
- 한계: `moveStage`라는 이름이 `'keep'`일 때는 정확하지 않다. 이름을 바꾸면 #3의 기록과 어긋나 파라미터로 두고 주석으로 보완했다.
- 관련: toggle-in-place(#15)
- 추가(2026-09-24, #12): 세 번째 규칙 `{ after: id | null }`을 더했다. 지정한 지원자 바로 뒤에 끼운다(`null`은 맨 앞, 못 찾으면 끝). 되돌리기와 실패 롤백이 카드를 원래 자리로 돌려놓을 때 쓴다. 서버와 화면이 `src/shared/order.ts`의 `insertAfter` 하나를 같이 쓴다 (주요 결정 #16).

### 11. 검색·필터의 병목은 계산이 아니라 재조정 — useDeferredValue
- 결정: `useMemo`로 필터하고 `useDeferredValue`로 목록 렌더를 낮은 우선순위로 미룬다. 입력란은 즉시 값을, 보드는 미룬 값을 쓴다.
- 이유: 1,000건을 훑는 비용은 마이크로초 단위라 문제가 아니다. **진짜 비용은 키를 누를 때마다 최대 1,000장의 카드를 다시 그리는 React 재조정이다.** 그래서 계산을 줄이는 디바운스가 아니라 렌더 우선순위를 낮추는 쪽을 골랐다.
- 대안과 기각 이유: 수동 디바운스 — 호출 횟수는 줄지만 결과가 늦게 나타나 반응이 단절된 느낌을 준다. 타이머 관리도 필요하다. `useMemo`만 — 먼저 측정해 볼 수도 있었으나, 미룬 값을 쓰는 비용이 React 내장 훅 한 줄이라 굳이 미룰 이유가 없었다. Web Worker — 계산이 병목이 아니므로 방향이 어긋난다.
- 측정: 6타 연속 입력에서 입력 반영이 45 · 7 · 6 · 6 · 6 · 6ms, 50ms를 넘는 긴 작업 0건.
- 곁들인 것: 목록이 한 박자 뒤처진 동안 보드를 옅게 표시해 멈춘 것이 아님을 알린다.
- 관련: search-filter(#7), virtualization(#13)

### 12. 확인 창은 네이티브 dialog 요소로
- 결정: `<dialog>`의 `showModal()`을 쓴다. 직접 만든 모달도, 일반화한 모달 컴포넌트도 쓰지 않는다.
- 이유: 포커스 트랩, ESC로 닫기, 배경 조작 차단, 최상위 레이어 배치를 브라우저가 맡는다. 이 넷은 직접 만들 때 가장 틀리기 쉬운 부분이고, 접근성 배점이 걸린 영역이다. 브라우저가 이미 정확히 하는 일을 다시 구현할 이유가 없다.
- 기본 포커스는 **취소** 버튼에 둔다. 합격도 불합격도 되돌리기 어려운 결정이라 Enter를 연타해도 어느 쪽으로도 확정되지 않게 한다.
- 대안과 기각 이유: 직접 만든 모달 — 동작을 완전히 제어할 수 있으나 위 넷을 모두 구현해야 한다. 재사용 모달 컴포넌트 — #8 상세는 drawer라 요구가 달라 지금 일반화하면 헛수고가 된다.
- 관련: result-confirm(#16)

### 13. 모바일은 탭 전환, 마크업은 하나로
- 결정: 768px 이하에서 컬럼 탭을 띄우고 고른 컬럼 하나만 보여준다. 탭은 **ARIA 탭 패턴이 아니라 일반 버튼 + `aria-current`**로 만들고, 데스크톱과 모바일이 **같은 마크업**을 쓴다.
- 탭 패턴을 쓰지 않은 이유: 탭 패턴은 좌우 화살표를 탭 전환에 배정한다. 이 앱에서는 좌우 화살표가 카드 이동이다(DECISIONS.md "가정: 키보드 조작 규칙"). 포커스 위치로 갈린다고는 해도 같은 화면에서 같은 키가 두 일을 하게 된다.
- 마크업을 하나로 둔 이유: `role`은 CSS로 바꿀 수 없지만 표시 여부는 바꿀 수 있다. 컬럼에 `data-active`를 두고 CSS가 보고 정하면 조건부 렌더링도 `matchMedia` 상태도 필요 없다. 데스크톱에서는 탭 막대가 `display: none`이라 접근성 트리에서도 사라진다.
- 가로 스크롤이 아니라 탭으로 간 이유: 카드를 옮긴 뒤 **그 카드를 눈으로 따라갈 수 있다.** 이동하면 보이는 컬럼도 함께 넘어가고, 되돌려지면 함께 되돌아온다. 가로 스크롤이면 옮긴 카드가 화면 밖 컬럼으로 사라져 어디로 갔는지 알 수 없다.
- 터치 영역: 이동 버튼과 결과 칩을 데스크톱 24px, 모바일 44px로 키웠다. WCAG 2.5.8은 마우스를 포함한 모든 입력에 24×24px 이상을 요구하는데 기존 22px는 데스크톱에서도 미달이었다. 읽기 전용 칩은 키우지 않았다. 누를 수 없는 것에 터치 기준을 적용할 이유가 없다.
- 관련: responsive(#17), a11y-keyboard(#10), virtualization(#13)

### 14. 롤백·경쟁 상태 테스트는 mock 모듈을 대체해 응답 순서를 테스트가 쥔다
- 결정: `vi.mock`으로 `moveStage`를 대체한다. 호출마다 대기 중인 Promise를 쌓아 두고, 어느 요청을 언제 성공·실패시킬지 테스트가 정한다. DOM 환경은 jsdom, 설정은 `vite.config.ts`를 건드리지 않는 별도 `vitest.config.ts`다.
- 이유: 실제 mock은 200~800ms 난수 지연과 15% 실패라서, 브라우저 실험에서 "12회 중 7회"처럼 확률로만 재현된다. 응답 순서를 테스트가 쥐면 "첫 요청 성공 + 두 번째 요청 실패" 같은 시나리오를 매번 같은 순서로 만들 수 있다. 직렬화는 "첫 요청이 끝나기 전에는 호출이 1건뿐"이라는 사실로 직접 확인한다.
- 소스 변경 예외: `applyMove`에 `export`를 붙였다. 테스트 이슈라 기존 소스를 되도록 바꾸지 않기로 했지만, 순수 함수를 입력/출력으로 직접 검증하는 쪽을 택했다. 동작은 바뀌지 않는다.
- 대안과 기각 이유: 실제 mock + 가짜 타이머 + `Math.random` 고정 — 실제 경로와 localStorage까지 보지만, 테스트가 mock 내부의 `Math.random` 호출 순서(지연 → 실패 판정)에 묶여 mock을 고치면 깨진다. 그래서 ⑤ "서버 쓰기 순서"는 localStorage 값이 아니라 moveStage 호출 순서로 확인한다. 이것이 이 선택의 한계다.
- 관련: rollback-test(#11), 주요 결정 #7·#8
### 14. 초성 검색은 검색어별 매처 함수로, 초성은 매번 계산
- 결정: `createChosungMatcher(keyword)`가 대상마다 호출할 함수를 돌려준다. 이름의 초성은 미리 저장하지 않고 비교할 때마다 계산한다.
- 이유: 음절 하나당 나눗셈 한 번이라 1,000건 × 3글자도 0.1ms 미만이다(측정). 미리 계산하면 카드를 옮길 때마다 배열이 바뀌어 캐시를 다시 만들어야 하고, 혼합 매칭은 음절 원문도 함께 봐야 해서 얻는 이점도 적다. 검색어에 초성이 있는지는 매처를 만들 때 한 번만 판정한다.
- 대안과 기각 이유: applicants 변경 시 id→초성 Map 미리 계산 — 위 이유로 기각. `(target, keyword)` 두 인자 함수 — 판정이 대상마다 반복되어 리뷰에서 매처 형태로 바꿨다.
- 관련: search-filter(#7), chosung-search(#18)
### 14. 상세는 읽기 전용 모달 drawer, 대상은 id로 찾는다
- 결정: 지원자 상세를 네이티브 `<dialog>`의 `showModal()`로 띄우고 오른쪽 시트로만 꾸민다(좁은 화면에서는 전체 화면). drawer는 **읽기 전용**이며 단계 이동은 카드에서만 한다. 보드는 지원자 객체가 아니라 **id만 들고 매 렌더마다 목록에서 찾는다.**
- 이유:
  - 모달로 두면 포커스 트랩·ESC·배경 차단을 브라우저가 맡는다(결정 12와 같은 근거). 대신 열려 있는 동안 보드를 조작할 수 없는데, 읽기 전용이라 잃는 것이 없다.
  - 읽기 전용이면 drawer 안에서 결과 확인 창이 겹치거나 롤백 중에 포커스를 옮기는 경로가 생기지 않는다.
  - id로 찾으면 열기 직전에 보낸 이동이 열려 있는 동안 롤백돼도 drawer가 실제 단계를 보여준다. 스냅샷으로 들고 있으면 화면이 실제 단계와 어긋난다. 브라우저에서 실패를 고정해 확인했다(PROMPTS.md [detail-panel]).
- 대안과 기각 이유: 비모달 `<aside>` 패널 — 열어 둔 채로 보드를 조작할 수 있으나 ESC·포커스 관리를 직접 구현해야 하고, 카드가 필터에 걸려 사라지는 경우까지 처리해야 한다. 열 때의 객체 스냅샷 — 더 단순하지만 롤백과 어긋난다.
- 관련: detail-panel(#8), a11y-keyboard(#10)

### 15. 로딩 화면은 실제 Column과 비활성 필터바로 그린다
- 결정: 첫 조회 동안 실제 `Column`에 회색 카드 자리를 넣은 `BoardSkeleton`을 보여준다. 필터바는 로딩 중에도 `disabled`로 띄운다. 재시도는 조회 effect의 의존성에 시도 번호를 넣어 effect를 다시 돌린다.
- 이유: 스켈레톤이 보드와 다른 모양이면 목록이 도착할 때 화면이 튄다. 컬럼을 재사용해 폭과 헤더를 맞췄지만, 측정해 보니 ready 때만 나타나는 필터바가 보드를 79px(모바일 91px) 밀어냈다. 실제 요소를 비활성으로 두면 높이를 숫자로 맞출 필요가 없다. 재시도는 기존 `cancelled` 가드를 그대로 쓰므로, 앞 시도의 늦은 응답이 새 시도를 덮지 않는다. 진행 중인 feat/undo와 충돌하지 않도록 `useApplicants` 변경을 10줄로 막았다.
- 대안과 기각 이유: 스피너 + 문구 — 레이아웃이 크게 바뀐다. 독립 스켈레톤 마크업 — Column이 바뀌면 모양이 어긋난다. 필터바 높이만큼 빈 자리 — 화면 폭마다 높이가 달라 어긋난다. load 함수 추출 — 취소 가드를 ref로 다시 만들어야 하고 조회 블록 전체가 바뀐다.
- 알려진 한계: BoardSkeleton이 `Board.module.css`·`ColumnTabs.module.css` 클래스에 기대므로 클래스 이름이 바뀌면 함께 깨진다(타입 검사로는 잡히지 않음).
- 관련: loading-error-empty(#9)

### 16. 되돌리기는 같은 이동 경로 + 앞 카드 기준 정확 복원
- 결정: 되돌리기를 별도 경로로 만들지 않는다. 이동 직전의 단계와 "같은 컬럼에서 바로 앞 카드 id"를 토스트에 담아 두었다가, 되돌릴 때 그 단계·`{ after: 앞 카드 }`로 한 번 더 이동한다.
- 이유: 기존 이동 경로를 타면 카드별 직렬화·늦은 응답 무시·실패 롤백(주요 결정 #7·#8)을 그대로 물려받는다. 위치는 사용자가 "원래 위치 정확 복원"을 골랐다. 인덱스를 기억하면 그 사이 다른 카드가 움직였을 때 밀리므로 앞 카드 id를 기준으로 삼았다. 컬럼은 전역 배열을 걸러낸 부분열이라 앞 카드 뒤에 끼우면 컬럼 안 순서가 돌아온다.
- 대안과 기각 이유: 원래 이동과 같은 placement(AI 추천) — 코드는 가장 작지만 컬럼 이동을 되돌리면 원래 순서가 아니라 맨 아래에 나타난다. 항상 `'end'` — 결과 토글을 되돌려도 카드가 튄다(#15에서 고친 문제의 재발).
- 세부: 되돌리기 토스트는 서버가 이동을 확정한 뒤에만 띄운다. 저장되지 않은 이동을 되돌리게 하면 실패 롤백과 겹쳐 카드가 어디로 갈지 알 수 없다.
- 같이 바뀐 것: 모든 이동의 실패 롤백이 "이동 직전 자리"로 돌아간다 (아래 가정 수정 참고).
- 관련: undo(#12)
### 15. 화살표 이동은 버튼과 같은 경로로, 포커스는 이동을 시작한 버튼 종류로 돌려준다
- 결정: `←`/`→`를 카드 `li`의 `onKeyDown`에서 받는다. detail-panel의 `↑`와 같은 자리다. 카드 안 어느 버튼(이름·칩·이동)에 포커스가 있든 동작하고, 이동은 버튼 클릭과 **같은 `onMove` 경로**를 탄다. 카드는 이동을 시작한 버튼의 종류(`FocusOrigin`)를 함께 넘기고, 보드는 새 위치에서 같은 종류의 버튼을 찾아 포커스를 준다. 이동 성공은 보드가 보이지 않는 `role="status"` 영역으로 알린다.
- 이유:
  - 같은 경로를 타므로 처우협의 → 결과 확인 창, 카드별 롤백, 늦은 응답 무시가 키보드에도 그대로 적용된다. 키보드 전용 분기를 만들면 둘이 어긋날 수 있다.
  - 포커스를 늘 이동 버튼으로 보내면, 이름 버튼에서 `→`를 누른 사용자는 다음 입력 때 다른 버튼 위에 있게 된다. 확인 창을 취소한 뒤에도 같은 규칙으로 돌아온다.
  - 성공 안내는 서버가 확정한 뒤(`confirmed`)에만 한다. 실패는 기존 토스트가, 뒤이은 요청에 밀린 응답은 그 요청이 알린다. 같은 문구가 이어져도 다시 읽히도록 번호를 key로 노드를 바꾼다.
- 대안과 기각 이유: 이름 버튼에서만 화살표 받기 — 실수는 줄지만 `↑` 규칙과 어긋난다(사용자 선택). 성공 안내를 `useApplicants`에서 만들기 — undo까지 자동으로 덮지만, 훅 테스트와 undo 브랜치가 같은 파일을 건드려 충돌 범위가 넓어진다(사용자 선택). 성공도 토스트로 띄우기 — 이동할 때마다 토스트가 쌓이고, 보는 사람에게는 카드가 옮겨간 것 자체가 안내다.
- 관련: a11y-keyboard(#10), 주요 결정 #7·#8·#14

## 가정 (요구사항이 모호한 지점)

### 가정: mock API는 자체 구현, 초기 데이터는 직접 생성
- 원문: 1장 "데이터는 제공되는 mock API(또는 로컬 mock 서버)를 통해 읽고 씁니다", 4장 "mock API — 자체 구현"
- 가정: 제공되는 mock API·데이터 파일은 없는 것으로 보고 자체 구현한다. 지원자 시드 데이터는 고정 seed 기반 생성기로 1,000건을 만든다.
- 근거: 원문 7장을 확인하지 못했고, 4장이 명시적으로 자체 구현을 허용한다.
- 영향: mock-api

### 가정: 15% 실패를 읽기와 쓰기 모두에 적용
- 원문: 2장 "mock API는 일정 확률로 실패하도록 설정되어 있음" — 어느 호출에 적용하는지는 명시되지 않았다.
- 가정: 조회와 이동 모두에 적용한다.
- 근거: 쓰기에만 적용하면 로딩 실패 에러 UI(Must 6, #9)를 실제 동작으로 보여줄 수 없고 별도 주입 장치가 필요해진다. 장치를 만들면 그것이 곧 요구사항 우회로 보일 수 있다. 같은 이유로 실패율을 낮추거나 끄는 수단도 두지 않았다.
- 영향: mock-api, card-list(#4), loading-error-empty(#9)

### 가정: 시드 데이터의 초기 단계 분포
- 원문: 초기 데이터 자체가 제공되지 않아 분포 규정도 없다.
- 가정: 서류검토 45% · 면접 25% · 처우협의 10% · 최종합격 8% · 불합격 12%로 앞단을 두껍게 생성한다. 고정 seed(20260923)를 써서 몇 번을 돌려도 같은 1,000건이 나온다.
- 근거: 실제 채용 파이프라인은 앞단이 두껍다. 첫 컬럼이 붐벼야 대량 데이터 성능(#13) 개선 효과가 눈에 보인다. 재현 가능한 시드는 평가자가 같은 데이터로 확인할 수 있게 한다.
- 영향: mock-api, virtualization(#13)

### 가정: 지원자 상세 필드
- 원문: 카드에는 "이름·직무·지원일·현재 단계"만 명시. 상세 보기의 내용은 미명시.
- 가정: 상세 패널에는 위 4개 + 이메일·연락처·경력연수·메모를 표시한다.
- 근거: 채용 담당자가 상세에서 볼 법한 최소 정보. 시드 생성기에서 만들기 쉽다.
- 영향: mock-api(데이터 모델), detail-panel

### 가정: 불합격은 5번째 독립 컬럼
- 원문: "서류검토 → 면접 → 처우협의 → 최종합격 / 불합격"
- 가정: 불합격을 최종합격과 나란한 5번째 컬럼으로 두고, 어느 단계에서든 불합격으로 보낼 수 있게 한다.
- 근거: 실제 채용에서 불합격은 특정 단계 다음이 아니라 모든 단계에서 발생하는 종착 상태다. 컬럼과 단계를 1:1로 두면 상태 모델이 단순해진다.
- 영향: board-layout, stage-move (구체적 이동 규칙은 stage-move에서 확정)

### 가정(임시, 2026-09-23 수정): 최종합격과 불합격을 "결과" 한 컬럼으로 합친다
- 원문: "단계는 서류검토 → 면접 → 처우협의 → 최종합격 / 불합격" — 슬래시로 묶여 있을 뿐 컬럼을 몇 개로 그릴지는 명시되어 있지 않다.
- **앞선 가정을 수정한 것이다.** 처음에는 불합격을 5번째 독립 컬럼으로 두고 5개를 선형으로 순회하기로 했다(커밋 c3af251). 그 가정으로 board-layout·stage-move·optimistic-update까지 구현했고, 히스토리에 그대로 남겨 둔다.
- 수정된 가정:
  1. 컬럼은 **서류검토 → 면접 → 처우협의 → 결과** 4개다.
  2. "결과" 컬럼은 최종합격과 불합격 지원자를 함께 담는다.
  3. 카드의 단계 칩을 누르면 최종합격 ↔ 불합격이 토글된다. 토글도 단계 변경이므로 mock API를 거치고 낙관적 반영·롤백을 똑같이 탄다.
  4. 처우협의에서 `→`를 누르면 기본값인 최종합격으로 들어간다. 불합격 처리는 칩 토글로 한다.
  5. 결과 컬럼에서 `→`는 무동작이고 `←`는 처우협의로 돌아간다.
- 근거 (사용자 판단): 최종합격과 불합격은 파이프라인의 다음 단계가 아니라 같은 지점의 두 결과다. 한 컬럼으로 묶으면 진행 단계가 더 명확해진다. 앞선 가정에서 지적했던 부작용 — 최종합격에서 `→` 한 번에 불합격이 되는 문제 — 도 함께 사라진다.
- 임시 가정이다. 과제 원문에 컬럼 구성 규정이 없어 지원자가 정한 것이고, 이후 판단이 바뀌면 다시 수정할 수 있다.
- 영향: board-layout(#2, 완료) · stage-move(#5, 완료) · detail-panel(#8) · a11y-keyboard(#10) · 새 이슈 #14
- 상태: #14에서 구현 완료. 저장되는 단계는 5개 그대로 두고 화면 컬럼만 4개로 묶었다 (주요 결정 #9).

### 가정: 필터 중 컬럼 헤더의 인원 수는 필터된 수를 보여준다
- 원문: 필터와 헤더 수치의 관계에 대한 규정이 없다.
- 가정: 검색·필터가 걸린 동안 컬럼 헤더는 **그 조건에 맞는 인원 수**를 보여준다. 단계별 전체 규모가 아니다.
- 근거: card-list에서 "헤더 수치 = 실제 카드 수"를 수용 기준으로 삼았다. 필터 중에만 어긋나면 화면의 카드 수와 숫자가 달라 혼란스럽다. 전체 규모는 필터를 해제하면 바로 보인다.
- 영향: search-filter(#7)

### 가정: 처우협의에서 결과로 넘어갈 때 합격 여부를 묻는다
- 원문: 확인 절차에 대한 규정이 없다.
- 가정: 처우협의에서 `→`를 눌러 결과 컬럼으로 넘어갈 때 "최종합격인가요?"를 묻고, **예**는 최종합격, **아니요**는 불합격으로 보낸다. 취소하거나 `ESC`로 닫으면 이동하지 않는다.
- 근거: 합격과 불합격은 되돌리기 어려운 결정인데 지금은 화살표 한 번에 최종합격이 되어 실수하기 쉽다. 불합격으로 하려면 결과 컬럼에서 칩을 한 번 더 눌러야 해 두 단계를 거친다. 확인 창을 두면 한 번에 정확한 결과로 보내면서 오조작도 막는다.
- **앞선 가정의 4번을 대체한다.** "처우협의에서 `→`는 기본값인 최종합격으로 들어간다"는 규칙이 확인 창으로 바뀐다. 칩 토글은 정정 수단으로 그대로 남는다.
- 브라우저 기본 `confirm()`은 쓰지 않는다. 렌더링을 막고 스타일·포커스·스크린리더 안내를 제어할 수 없다. 앱 안에 모달을 만든다.
- 적용 범위: 처우협의 → 결과 이동에만. 다른 컬럼 간 이동과 결과 칩 토글에는 확인 창이 뜨지 않는다.
- 영향: result-confirm(#16), a11y-keyboard(#10)
- 상태: #16에서 구현 완료. 네이티브 `<dialog>`로 만들었다 (주요 결정 #12).

### 가정(2026-09-24 수정): 결과 토글은 자리를 지키고, 컬럼 이동만 맨 아래로 간다
- 원문: 결과 전환이라는 개념 자체가 지원자가 만든 것이므로 규정이 없다.
- **처음 가정(#14, 폐기)**: 토글도 단계 변경이므로 카드가 결과 컬럼 맨 아래로 간다. mock API가 모든 단계 변경에서 카드를 배열 끝으로 보내므로 화면도 따르는 편이 일관된다고 봤고, "방금 결정한 지원자가 맨 아래"라는 해석도 가능하다고 적었다.
- **수정된 가정(#15)**: 컬럼 **간** 이동만 맨 아래로 보내고, 컬럼 **안** 결과 전환은 자리를 지킨다.

  | 동작 | 위치 |
  |---|---|
  | 컬럼 간 이동 (← →) | 대상 컬럼 맨 아래 |
  | 결과 토글 (칩) | 제자리 |

- 수정한 이유: 실제로 써 보니 받아들이기 어려웠다. 200장 넘는 결과 컬럼에서 방금 토글한 카드가 화면 밖으로 나가 다시 찾으러 스크롤해야 한다. 다른 컬럼으로 옮기는 것은 "새로 도착한 카드"라 맨 아래가 맞지만, 같은 컬럼 안에서 결과만 바꾸는 것은 도착이 아니라 정정이므로 자리가 움직일 이유가 없다.
- 함께 고친 것: 화면에서만 자리를 지키면 서버는 여전히 끝으로 보내 **새로고침했을 때 카드가 튄다.** 문제가 없어지는 게 아니라 새로고침까지 미뤄질 뿐이다. 그래서 mock API에도 같은 구분을 넣었다 (주요 결정 #10). 롤백도 같은 자리 규칙을 쓴다. 실패했을 때만 카드가 튀면 더 이상하다. (→ #12에서 롤백은 "이동 직전 자리"로 수정됨. 아래 롤백 가정 참고)
- 영향: stage-merge(#14) → toggle-in-place(#15)

### 가정(2026-09-24 수정): 롤백된 카드는 이동 직전 자리로 돌아간다
- 원문: 실패 시 "원상 복구"라고만 되어 있고 목록 안 위치는 언급이 없다.
- **처음 가정(optimistic-update, 폐기)**: 단계만 되돌리고 위치는 그 컬럼의 맨 아래로 한다. "방금 움직인 카드가 맨 아래"라는 규칙과 일관되고, 원래 인덱스를 되돌리려면 앞 항목 id 기준 추적이 필요해 얻는 것에 비해 복잡하다고 봤다.
- **수정된 가정(#12)**: 이동 직전의 자리(같은 컬럼의 앞 카드 뒤)로 돌아간다.
- 수정한 이유: 되돌리기(#12)를 만들면서 앞 카드 id 추적이 이미 생겨 "복잡하다"는 근거가 사라졌다. 그리고 처음 가정에는 결함이 있었다. 실패한 요청에서 서버는 카드를 움직이지 않았는데 화면만 맨 아래로 보내므로, **새로고침하면 카드가 원래 자리로 튄다.** 자가 리뷰 4번으로 지적했고 사용자가 채택했다. 헤드리스 Chrome에서 실패 롤백 후 화면 순서 = 저장 순서 = 이동 전 순서를 확인했다.
- 예외(2026-09-24, main 병합 후 fix): 제자리 이동(`'keep'`, 결과 토글)의 실패는 `'keep'`으로 되돌린다. 카드가 자리를 떠난 적이 없기 때문이다. 앞 카드 기준으로 다시 끼우면 컬럼 순서는 같아도 전체 목록에서 자리가 바뀌어, rollback-test(#11)의 "keep 이동이 실패하면 제자리에서 되돌린다"가 깨졌다. toggle-in-place(#15)에서 사용자가 요청한 동작이다.
- 영향: optimistic-update → undo(#12)

### 가정: 되돌리기 범위와 시간 (#12)
- 원문: "되돌리기(Undo): 방금 한 이동을 취소." 깊이·시간·키보드는 언급이 없다.
- 가정 (사용자 선택):

  | 항목 | 가정 |
  |---|---|
  | 깊이 | 성공한 이동마다 되돌리기 토스트 1개. 떠 있는 동안은 여러 건을 각각 되돌릴 수 있다 (토스트 최대 4개) |
  | 시간 | 토스트 표시 시간 5초. 마우스를 올리거나 포커스를 두면 멈춘다 |
  | 끼어든 이동 | 같은 카드를 다시 옮기면 그 카드의 옛 되돌리기는 즉시 사라진다. "방금 한 이동"이 아니게 되었기 때문이다 |
  | 키보드 | 토스트 버튼(Tab) + Ctrl/⌘+Z(가장 최근 1건). 입력창·확인 창에서는 가로채지 않는다 |
  | 성공 피드백 | 없음. 카드가 돌아온 것이 피드백이며 되돌리기의 되돌리기(redo)는 만들지 않는다 |
  | 실패 피드백 | "되돌리지 못했습니다. ○○ 단계에 그대로 둡니다." (일반 이동 실패 문구와 구분) |
  | 앞 카드가 사라진 경우 | 전역 배열에서 그 카드 바로 뒤, 못 찾으면 끝. 앞 카드가 다른 컬럼 끝으로 갔다면 되돌린 카드도 원래 컬럼 아래쪽에 놓인다 |

- 한계: 되돌린 뒤 모바일 탭은 카드를 따라가지 않고, 포커스는 남은 되돌리기 버튼으로만 옮긴다. `Board.tsx`를 건드리지 않는다는 작업 범위에 따라 사용자가 범위 밖으로 정했다. 그래서 좁은 화면에서는 다른 탭에 있는 카드가 되돌아가면 눈에 보이지 않는다.
- 영향: undo(#12)

### 가정: 키보드 조작 규칙 (지원자가 직접 정의)
- 원문: 3장에 "웹 접근성 — 키보드만으로 단계 이동과 상세 열람이 가능해야 함"만 있고, **구체적인 키 바인딩은 명시되어 있지 않다.** 원문 9장("요구사항이 모호하면 스스로 가정하고 그 가정을 DECISIONS.md에 명시")에 따라 지원자가 아래와 같이 정의한다.
- 가정:
  1. 카드를 선택한 뒤 `←` / `→` 로 이전 / 다음 단계로 이동한다.
  2. 첫 컬럼(서류검토)에서 `←`, 마지막 컬럼(불합격)에서 `→` 는 아무 동작도 하지 않는다. 순환하지 않는다.
  3. 이동한 카드는 대상 컬럼의 **마지막 인덱스**에 놓인다.
  4. `↑` 로 상세 drawer를 연다. `ESC` 또는 `↓` 로 닫는다.
  5. 화살표 이동은 불합격을 포함한 5개 컬럼을 선형으로 순회한다.
- 근거:
  - 칸반 보드는 좌→우가 곧 진행 방향이라 좌우 화살표가 단계 이동에 직관적이다.
  - 경계에서 순환시키면 서류검토에서 `←` 한 번에 불합격이 되는 사고가 난다. 그래서 경계는 무동작으로 막는다.
  - 마지막 인덱스 배치는 "방금 들어온 카드가 맨 아래"라는 일관된 규칙이라 사용자가 이동 결과를 찾기 쉽다.
  - 불합격만 별도 키로 빼면 규칙이 둘로 갈라져 학습 부담이 는다. 선형 규칙 하나로 유지한다.
- 인지한 부작용: 최종합격에서 `→` 를 한 번 누르면 불합격이 된다. 되돌리기(#12)로 복구 가능하게 하여 완화한다.
- 영향: stage-move(#5) — 역방향 이동을 허용한다는 뜻이기도 하다 / detail-panel(#8) — drawer 형태로 확정 / a11y-keyboard(#10)
- 상태(2026-09-24, #10 구현): 4컬럼 구조에서 2번의 "마지막 컬럼"은 **결과 컬럼**이다. 5번의 선형 순회는 컬럼 단위가 되었고, 처우협의에서 `→`는 확인 창을 거친다. 1번의 "카드를 선택한 뒤"는 **카드 안 어느 버튼에 포커스가 있든**으로 해석했다(사용자 선택). 키를 누르고 있어 생기는 반복 입력은 무시한다. 한 번 누르면 한 칸이다.

### 가정: 상세 보기를 여는 방법과 범위
- 원문: "카드 클릭 시 지원자 상세(사이드 패널 또는 모달)". 어디를 누르는지, 열린 상세에서 무엇을 할 수 있는지, URL에 남는지는 미명시.
- 가정:
  1. 카드의 이름이 버튼이다. 마우스로는 카드의 빈 곳을 눌러도 열린다. 카드 안의 칩·이동 버튼, 텍스트를 드래그해 선택한 상태의 클릭은 열지 않는다.
  2. 카드 안 어느 버튼에 포커스가 있든 `↑`로 연다. `ESC`·`↓`·✕·배경 클릭으로 닫는다 ("가정: 키보드 조작 규칙" 4번을 이번에 구현).
  3. 닫으면 어느 경로로 열었든 그 카드의 이름 버튼으로 포커스가 돌아간다.
  4. 상세는 읽기 전용이다. 단계 이동은 카드에서만 한다.
  5. URL에 반영하지 않는다. 새로고침하면 닫힌다.
- 근거: 이름을 진짜 버튼으로 두면 키보드·스크린리더 경로가 저절로 생기고, 카드 전체를 버튼으로 만들면 안에 든 버튼이 중첩돼 접근성이 깨진다. 라우터가 없는 SPA라 URL 동기화는 비용 대비 이득이 작다.
- `↓`가 drawer 안의 키보드 스크롤과 겹치는 점은 인지했다. 키 규칙을 한 가지로 유지하려고 받아들였다 ("AI 제안 채택/기각" 참조).
- 영향: detail-panel(#8), a11y-keyboard(#10) — `←`/`→`는 같은 카드 `onKeyDown`에 붙이면 된다

### 가정: 제출은 공개 GitHub 레포 링크
- 원문: 제출 형식 미확인(7장 없음).
- 가정: 공개 레포 `gygy7151/recruitment-pipeline-board`의 링크와 커밋 히스토리로 제출한다.
- 영향: init (레포 public)

### 가정: 초성과 완성 음절이 섞인 검색어는 글자별로 맞춘다
- 원문: 초성 검색 규정이 없다. 이슈 #18 초안은 "완성된 음절이 섞여 있으면 지금처럼 이름 부분 일치"였다.
- 가정: 검색어의 초성 글자는 대상 음절의 초성과, 완성 음절은 같은 음절과 비교한다. `한ㅅ` → 한수빈.
- 근거: IME로 "한수"를 치면 중간에 반드시 `한ㅅ` 상태를 거친다. 이슈 초안대로라면 그 순간 결과가 0건으로 깜빡인다. 초성이 없는 검색어의 결과는 기존과 같으므로 잃는 것이 없다.
- 영향: chosung-search(#18)

### 가정: 로드 실패는 수동 재시도로만 복구한다
- 원문: 2장 Must 6 "로딩/에러/빈 상태 — 세 가지 상태를 모두 UI로 처리"
- 가정: 자동 재시도 없이 에러 UI의 "다시 시도" 버튼으로만 다시 부른다. 에러 메시지는 원인과 상관없이 한 문장이다.
- 근거: 자동 재시도를 넣으면 15% 실패가 화면에 거의 드러나지 않는다. 그러면 에러 UI 요구를 확인하기 어렵고, 훅 변경도 커진다.
- 영향: loading-error-empty

### 가정: 빈 컬럼은 한 문구, 보드 전체 0건은 안내 + 조건 초기화
- 가정: 비어 있는 컬럼은 필터 여부와 상관없이 "이 단계에 지원자가 없습니다."를 보여준다. 검색 결과가 보드 전체에서 0건이면 #7처럼 보드 대신 안내를 띄우고 "검색 조건 초기화" 버튼을 둔다.
- 근거: 문구를 필터 여부로 나누려면 Board.tsx가 Column에 값을 넘겨야 하는데, Board.tsx는 병렬 브랜치(feat/detail-panel)가 쓰고 있다. 필터 중에는 필터바의 결과 수 안내가 이미 있다. 0건일 때 빈 컬럼 4개를 늘어놓기보다 한 곳에서 원인과 되돌릴 방법을 알려준다.
- 영향: loading-error-empty, search-filter(#7)

## AI 제안 채택/기각

### 기각: 가로 스크롤 오른쪽 여백 우회 코드
- 제안: 자가 리뷰에서 "WebKit/Blink는 가로 스크롤 flex 컨테이너의 오른쪽 패딩을 스크롤 영역에 포함하지 않는다"고 지적하고, CSS 변수 2개와 `::after` 스페이서로 우회하는 코드를 넣었다. (출처: board-layout 리뷰 1번)
- 결정: 채택하지 않음. 원래의 `padding: 16px 24px 24px`로 되돌림.
- 이유: 추측을 기록할 수 없어 Chrome DevTools Protocol로 실제 측정했더니 **Chrome 154에서 재현되지 않았다.** 두 방식 모두 스크롤 끝 여백이 24px로 동일하다. 재현되지 않는 버그의 우회 코드는 불필요한 복잡도다. (측정 표는 PROMPTS.md [board-layout] 참조)
- 남은 한계: Safari(WebKit)는 헤드리스 자동화에 사용자 설정 변경이 필요해 확인하지 못했다.

### 이월: 컬럼 본문의 키보드 스크롤
- 제안: 컬럼 본문이 `overflow-y: auto`인데 포커스를 받지 못해 키보드만으로 스크롤할 수 없다 (WCAG 2.1.1). (출처: board-layout 리뷰 4번)
- 결정: a11y-keyboard 단계로 이월.
- 이유: 카드가 포커스 가능해지면 포커스 이동으로 스크롤되어 저절로 해결될 수 있다. 지금 `tabindex="0"`을 주면 빈 컬럼까지 탭 순회에 들어가 탭 이동만 길어진다. 카드를 올린 뒤 실측해서 판단한다.
- 실측 결과(a11y-keyboard): 면접 컬럼 첫 카드에서 실제 Tab을 30번 누르자 본문 `scrollTop`이 0 → 397px로 따라왔고, 포커스를 받은 버튼이 컬럼의 보이는 영역 안에 있었다. **본문에 `tabindex`를 주지 않는다.** 한계: 200번째 카드까지 가려면 Tab을 수백 번 눌러야 한다. 이는 스크롤 문제가 아니라 목록 길이 문제라 virtualization(#13)과 검색으로 다룬다.

### 채택: 리팩터링으로 쓰임이 없어진 export 제거
- 제안: `Stage` 타입이 아무 데서도 쓰이지 않는다. `Column` 컴포넌트가 `BoardColumn`을 쓰게 되면서 마지막 사용처가 사라졌다. (출처: stage-merge 리뷰 1번)
- 결정: 채택. `StageId`를 `Stage['id']` 대신 `(typeof STAGES)[number]['id']`로 직접 뽑아 쓰게 고쳤다.
- 이유: 미사용 export 문제의 네 번째지만 성격이 다르다. 앞의 셋(StageId, mock API 상수 5개, LoadStatus)은 새로 만들면서 붙인 것이고, 이번엔 리팩터링으로 쓰임이 없어진 것이다. **"새로 만들 때 확인한다"는 체크리스트만으로는 잡히지 않는다.** 남의 사용처를 뺏는 변경을 할 때도 세어야 한다.

### 채택: 포커스 복원을 목록 변경이 아니라 핸들러에서
- 제안: `useRef` + 목록 변경 효과로 포커스를 되돌리면 동시 이동에서 대상이 덮이고, 실패 시에는 복원이 아예 돌지 않는다. 각 클릭 핸들러가 직접 되찾자. (출처: stage-move 리뷰 1·2번)
- 결정: 채택. ref와 효과를 없애고 핸들러가 `await` 뒤 `requestAnimationFrame`에서 자기 카드의 버튼을 찾는다.
- 이유: 호출마다 별도 클로저라 이동이 겹쳐도 서로 섞이지 않는다. 성공과 실패가 같은 경로를 타므로 15% 실패에서도 포커스가 body에 갇히지 않는다. 브라우저 측정으로 이동 중 `activeElement`가 실제 `BODY`임을 확인했다.

### 채택: 이동 버튼에서 화살표 래퍼 제거
- 제안: 버튼 안의 `aria-hidden` `span`이 DOM을 2,000노드 늘린다. `aria-label`이 있으면 불필요하다. (출처: stage-move 측정)
- 결정: 채택. 추측하지 않고 Chrome 접근성 트리에서 이름 출처를 읽어 확인한 뒤 제거했다.
- 이유: 접근 가능한 이름이 `aria-label` 속성에서만 나오고 내용은 우선순위에서 밀린다. 제거해도 이름이 동일했고 DOM은 10,053 → 8,053노드로 줄었다. 가상 스크롤(#13) 판단 기준선이 가벼워진다.

### 기각: 미사용 export를 잡는 도구(knip) 도입
- 제안: 미사용 export 실수가 세 번 반복됐으니(StageId → mock API 상수 5개 → LoadStatus) 도구로 고정하자. (출처: card-list 리뷰 2번)
- 결정: knip을 설치해 실제로 돌려본 뒤 제거했다.
- 이유: knip은 **자기 파일 안에서 참조되는 export를 사용된 것으로 판정한다.** 세 건 중 `StageId` 한 건만 잡고 나머지 둘은 못 잡는다. 설정을 바꿔도 결과가 같았고, 프로브 타입으로 탐지 자체는 동작함을 확인했으므로 설정 실수가 아니다. 반대로 곧 쓸 `moveStage`·`MockApiError`를 미사용으로 잡아 오탐이 났다. 한 건 잡고 두 건 오탐인 도구에 의존성을 추가할 이유가 없다.
- 대신 한 것: 규칙을 리뷰 체크리스트에 명시했다. "export 키워드마다 파일 밖 사용처를 grep으로 실제로 센다." 세 건 모두 실제로는 이 방식으로 잡혔다.

### 채택: card-list의 단계 값 검증과 목록 의미
- 제안: 저장소에 알 수 없는 단계가 있으면 카드가 조용히 사라지므로 검증하자. `ul`에 `role="list"`를 주자. (출처: card-list 리뷰 1·3번)
- 결정: 둘 다 채택. 검증은 화면이 아니라 **mock API의 복원 경계**에 넣었다.
- 이유: 화면에서 미분류 카드를 따로 그리면 mock 데이터에 비해 구조가 과하다. 입구에서 막으면 화면 코드는 단순한 채로 데이터 손실이 없다. `role="list"`는 `list-style: none` 때문에 Safari VoiceOver가 목록 의미를 없애는 것을 보완한다.

### 채택: mock API의 미사용 export 제거와 복원 시 형태 검증
- 제안: `SEED` `SEED_COUNT` `MIN_DELAY_MS` `MAX_DELAY_MS` `FAILURE_RATE`가 자기 파일 밖에서 쓰이지 않으므로 export를 떼고, localStorage 복원 시 필수 필드를 검사하자. (출처: mock-api 리뷰 1·2번)
- 결정: 둘 다 채택.
- 이유: 앞 사이클에서 `StageId`를 같은 이유로 지웠다. 같은 기준을 적용하지 않으면 모순되는 패턴이 남는다. export를 떼도 수용 기준의 "지연·실패율이 상수로 드러남"은 그대로 충족된다. 형태 검증은 1,000건 전수가 아니라 첫 항목 샘플 검사로 두어 비용을 들이지 않았다.

### 채택: 미사용 타입·CSS 중복·뷰포트 단위
- 제안: 미사용 `StageId` 타입 제거, `flex: 0 0 280px`와 중복된 `min-width` 제거, 구식 `clip` → `clip-path`, `100vh` → `100dvh`. (출처: board-layout 리뷰 2·3·5번)
- 결정: 모두 채택.
- 이유: 미사용 코드 방치는 과제가 명시한 감점 신호다. 나머지는 동작 변화 없이 일관성만 올린다. `StageId`는 mock-api 단계에서 실제로 쓸 때 다시 추가한다.

### 채택: 되돌리기 단축키와 토스트 시간 (#12)
- 제안: (1) 한글 입력 상태에서 `event.key`가 `'ㅋ'`라 Ctrl+Z가 안 먹을 수 있으니 `event.code`도 보자. (2) 키 반복으로 쌓인 되돌리기가 연달아 실행되니 `event.repeat`를 무시하자. (3) 버튼까지 Tab으로 가는 동안 5초가 지나면 되돌릴 수 없으니 hover/focus 중 시간을 멈추자 (WCAG 2.2.1). (출처: undo 리뷰 1·2·3번)
- 결정: 모두 채택.
- 확인: 헤드리스 Chrome에 `key='ㅋ'`, `code='KeyZ'`, `autoRepeat`를 실제 키 이벤트로 보내 확인했다. 시간 정지는 대조군(조작 없음 → 4,991ms에 사라짐)을 먼저 재고 포커스·hover 중 7초 유지를 확인했다.

### 채택: 일반 이동 실패 롤백도 이동 직전 자리로 (#12)
- 제안: `'end'` 이동이 실패하면 화면은 맨 아래로, 서버는 원래 자리에 있어 새로고침하면 카드가 튄다. 되돌리기용 자리 계산을 일반 롤백에도 쓰자. (출처: undo 리뷰 4번)
- 결정: 채택. optimistic-update의 가정을 수정했다 (위 "가정(2026-09-24 수정)").
- 이유: 기존 결정의 결함을 고치는 것이라 되돌리기 커밋에 함께 넣었다. 이동 직전 자리 계산을 `startMove` 한 곳에 두어, 되돌리기와 일반 이동이 같은 규칙으로 롤백한다.

### 채택: 인자 옵션 객체, `insertAfter`를 `shared/`로 (#12)
- 제안: `sendMove`/`startMove`의 인자가 6개·5개로 늘었으니 객체로 묶자. `insertAfter`가 mock(서버) 모듈에서 화면으로 export되어 경계가 흐리니 `shared/`로 옮기자. (출처: undo 리뷰 5·6번)
- 결정: 둘 다 채택. 4번과 합치니 이동 직전 자리 계산이 `startMove` 안으로 들어가 인자는 `{ recordUndo }` 하나가 되었다. `insertAfter`는 `src/shared/order.ts`에서 서버와 화면이 같이 쓴다.

### 채택: 되돌리기 실패 전용 문구 (#12)
- 제안: 검증 중 되돌리기 실패 토스트가 "이동하지 못해 면접 단계로 되돌렸습니다"로 떠 "되돌리기를 되돌렸다"로 읽혔다. (출처: undo 브라우저 검증 D)
- 결정: 채택. "되돌리지 못했습니다. 면접 단계에 그대로 둡니다."로 나눴다. 새 필드 없이, 되돌리기 요청에는 되돌리기 대상을 넣지 않는다는 점으로 구분한다.

### 채택: rollback-test 리뷰 3건
- 제안: (1) 기록만 하고 검증하지 않는 `PendingCall.placement` 필드를 단언에 사용 (2) RTL의 필수 peer인 `@testing-library/dom`을 devDependencies에 명시 (3) `keep`으로 옮겼다가 롤백할 때 카드가 제자리에 남는지 확인하는 테스트 추가. (출처: rollback-test 리뷰 1·2·3번)
- 결정: 사용자가 세 건 모두 채택했다.
- 제안 근거: (1) 미사용 필드는 감점 신호이고, 훅이 placement를 서버에 그대로 넘기는지 확인하는 김에 쓸 수 있다. (2) npm의 peer 자동 설치에 기대면 다른 패키지 매니저나 lock 재생성에서 깨진다. (3) toggle-in-place에서 고친 동작을 지키는 테스트가 없었다.
### 채택: 초성 유틸의 미사용 export 제거 (1b 유지안과 비교 후)
- 제안: `isChosung`·`getChosung`의 파일 밖 사용처가 0이므로 export를 떼자. (출처: chosung-search 리뷰 1번)
- 결정: 채택. "#11 단위 테스트를 위해 유지"하는 안과 장단점을 비교한 뒤 골랐다.
- 이유: 테스트 러너가 아직 없어 테스트용 export를 쓰는 코드가 없다. 공개 매처를 `ㅎㅅㅂ`·`ㄳ`·`ㄲ` 케이스로 테스트하면 초성 계산식도 간접적으로 전부 검증된다. #11에서 필요해지면 그때 export한다.

### 채택: 초성 판정을 검색어당 한 번으로 (처음엔 선택하지 않음)
- 제안: 검색어에 초성이 있는지를 지원자마다가 아니라 필터당 한 번만 판정하자. 순회 단위도 코드 유닛으로 통일하자. (출처: chosung-search 리뷰 2·3번)
- 결정: 3번은 처음부터 채택. 2번은 처음 선택에서 빠졌지만, 장단점 분석을 본 뒤 채택으로 바꿨다.
- 반영 방식: 인자를 늘리지 않고 `createChosungMatcher(keyword)`가 매처 함수를 돌려주는 형태로 바꿨다.

### 이월: 검색 placeholder에 초성 검색 안내, IME 중간 음절 매칭
- 제안: placeholder를 "이름 또는 초성(ㅎㅅㅂ)"으로 바꾸자. "하"(→"한" 입력 중)에서도 "한수빈"이 맞도록 마지막 음절을 분해하자. (출처: chosung-search 리뷰 4·5번)
- 결정: 이월. 앞의 것은 이 사이클의 수정 범위(`useApplicantFilter.ts` + 새 한글 유틸) 밖인 `FilterBar.tsx`를 바꿔야 한다. 뒤의 것은 이 기능 전부터 있던 동작이고 범위가 커진다.
### 채택: 텍스트를 선택한 채 누른 클릭은 상세를 열지 않는다
- 제안: 카드 빈 곳 클릭으로 상세가 열리므로, 이름이나 이메일을 복사하려고 드래그하면 마우스를 뗄 때 drawer가 뜬다. 선택 영역이 있으면 열지 않자. (출처: detail-panel 리뷰 1번)
- 결정: 채택. 클릭 핸들러에 `getSelection()` 검사 한 줄.
- 이유: 비용이 한 줄이고 오작동이 사라진다. 선택이 있을 때 열리지 않고, 선택을 풀면 같은 클릭으로 열리는 것을 브라우저에서 짝으로 확인했다.

### 기각: 목록에서 사라진 상세 id를 렌더 중에 비우는 코드 제거
- 제안: `if (detailId && !detailApplicant) setDetailId(null)`은 지금 UI로는 일어나지 않는 경우를 막는다. 모달이 열려 있으면 필터를 바꿀 수 없고, 목록이 비면 보드가 언마운트된다. 죽은 방어 코드이니 지우자. (출처: detail-panel 리뷰 2번)
- 결정: 기각. 남긴다.
- 이유(사용자): 방어 코드로 남긴다. 목록이 바뀔 수 있는 경로(폴링, 비모달 전환)가 생기면 id가 남아 있다가 같은 지원자가 다시 나타날 때 drawer가 저절로 열리는 버그를 막는다.

### 기각: 스크롤할 내용이 남아 있으면 `↓`로 닫지 않기
- 제안: `↓` 닫기가 drawer 안의 키보드 스크롤을 가로챈다. 긴 메모가 있는 좁은 화면에서는 문제가 된다. 내용이 끝까지 스크롤됐을 때만 닫자. (출처: detail-panel 리뷰 3번)
- 결정: 기각.
- 이유(사용자): 키 규칙을 한 가지로 유지한다. `↓`가 상황에 따라 다르게 동작하면 키보드 가정이 흔들린다. 지금 내용은 짧아 스크롤할 일이 거의 없다.

### 채택: loading-error-empty 리뷰 4건, 결합 1건은 유지
- 제안: (1) 스켈레톤의 `aria-busy` 제거 (2) 사라지는 버튼 대신 다음 대상으로 포커스 이동 (3) 스켈레톤에 모바일 탭 막대 자리 (4) reload 단위 테스트 (5) BoardSkeleton이 Board.module.css를 가져다 쓰는 결합 정리. (출처: loading-error-empty 리뷰 1~5번)
- 결정: 사용자가 (1)~(4)를 채택하고 (5)는 그대로 두었다. (4)는 병렬 브랜치와의 충돌을 피하려고 별도 테스트 파일로 뒀다.
- (5)를 둔 이유: 레이아웃 정의가 한 곳에 있어야 스켈레톤과 보드가 어긋나지 않는다. 클래스 이름이 바뀌면 화면에서 바로 드러난다.
### 채택: a11y-keyboard 리뷰 3건
- 제안: (1) 키를 누르고 있을 때의 반복 입력(`event.repeat`) 무시 (2) 결과 칩 토글 안내를 "옮겼습니다"가 아니라 "변경했습니다"로 (3) `.srOnly` 중복을 CSS Modules `composes`로 제거. (출처: a11y-keyboard 리뷰 1·2·3번)
- 결정: 사용자가 세 건 모두 채택했다.
- 제안 근거: (1) `→`를 누르고 있으면 카드가 처우협의까지 달려가 확인 창이 뜬다. (2) 토글은 자리를 지키는 정정이다(가정 "결과 토글은 자리를 지키고"). (3) 같은 규칙이 두 파일에 있으면 한쪽만 고쳐지는 일이 생긴다.

### 기각: 이름 버튼에 `aria-keyshortcuts`
- 제안: 화살표 단축키가 접근성 트리에 드러나지 않으니 이름 버튼에 `aria-keyshortcuts="ArrowLeft ArrowRight ArrowUp"`을 붙이자. (출처: a11y-keyboard 리뷰 4번)
- 결정: 기각.
- 이유(사용자): "단축키 안내는 README에 적어주면 좋겠어". README "키보드 조작" 절에 표로 적었다.

## 못 끝낸 기능

### 가상 스크롤(#13) — 착수하지 않았다
- 남긴 이유: `Board`와 `Column`을 크게 고쳐야 해서 모바일 대응(#17), 상세 패널(#8)과 같은 파일을 놓고 부딪힌다. 병렬 진행 중에는 시작할 수 없었다.
- 측정으로 확인한 현재 상태: 1,000건에서 **50ms를 넘는 긴 작업이 0건**이다. 검색 입력 반영이 6~45ms, 컬럼 스크롤 20회가 0.1ms다. `useDeferredValue`로 입력 반응성 문제가 해소되어 체감 지연이 없다.
- 남은 근거: 모바일에서 보이는 컬럼은 하나인데 숨겨진 컬럼 3개에 버튼 1,414개가 DOM에 남는다(전체 8,095노드). 포커스는 받지 않지만 재조정 비용은 그대로다. 가상 스크롤보다 "보이지 않는 컬럼은 렌더하지 않기"가 더 직접적인 해법일 수 있다.
- 대신 한 판단: 측정값이 기준을 넘지 않는데 구조를 크게 바꾸는 것보다, 근거를 남겨 두고 착수하지 않는 쪽을 택했다.

