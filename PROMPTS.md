# PROMPTS.md

프롬프트 & 리뷰/검증 로그. 기능 단위로 기록하며 커밋과 1:1로 대응한다.
AI 도구: Claude Code. 기능마다 사용한 모델을 섹션 첫 줄에 표기한다 (init·스킬 설계: Claude Fable 5.1, 이후 기능 구현: Claude Opus 5 등). 각 기능은 `/feature-step <feature>` 스킬 사이클
(요구사항 해석·가정 → 설계 선택 → 구현 → 독립 AI 리뷰 → 테스트 → 기록 → 커밋)로 진행한다.

---

## [init] 프로젝트 셋업

모델: Claude Fable 5.1

### 프롬프트 1
> 이 스킬은 내 깃허브에도 연결 되어 있어야해

### 프롬프트 2
> repo 이름은 recruitment-pipline-board로 할거야

### 프롬프트 3
> README 제목은
> Recruitment Pipeline Board
> 채용 단계별 지원자 관리 및 파이프라인 보드
> 이렇게 잡아줘 ..

### 프롬프트 4
> recruitment-pipeline-board로 고쳐줘, 그리고 init 시작하자

(AI가 레포 이름 `pipline`과 README 제목 `Pipeline`의 표기 불일치를 지적 → 사용자가 `pipeline`으로 확정)

### 프롬프트 5 — 초기 결정 (AI가 제시한 선택지에서 사용자가 선택)
- 프레임워크: Vite + React + TS (대안: Next.js)
- 상태 관리: React state + custom hook (대안: Zustand / Redux Toolkit / TanStack Query)
- 스타일링: CSS Modules (대안: Tailwind / styled-components)
- 단계 이동 방식: 액션 버튼 (대안: DnD + 키보드 대체 경로 / 버튼 먼저 후 DnD 추가)
- 레포 공개 범위: public
- 원문 7장(제출물·제공 mock API): 못 봤음 → 가정으로 진행
- 상세 패널 추가 필드: 이메일·연락처·경력연수·메모

### AI 출력 요지
- `npm create vite@latest -- --template react-ts`로 스캐폴딩 (React 19, TS 6, Vite 8, oxlint).
- `package.json`에 `typecheck` 스크립트와 `engines.node >= 20` 추가, `.nvmrc` 생성.
- README / DECISIONS.md / PROMPTS.md 스켈레톤 생성. DECISIONS.md에 초기 결정 4개와 가정 3개 기록.
- `git init` → 첫 커밋 → `gh repo create --public` 으로 원격 생성·push.

### 리뷰 / 검증
- 무엇이 문제였나: 첫 스캐폴딩 명령이 실패했다. 셸 기본 Node가 v16.16.0이어서 최신 create-vite가 실행되지 않았다.
- 어떻게 알아챘나: npm 에러 로그 확인 후 `node --version`으로 재현. fnm에 Homebrew Node 25.8.0이 `system`으로 있어 그것으로 재실행.
- 채택 / 수정 / 기각: 스캐폴딩 결과는 그대로 채택. Node 요구 버전을 프로젝트에 명시(`engines`, `.nvmrc`, README)하도록 수정.
- 어떻게 고쳤나: `PATH`에 Homebrew Node를 앞세워 재실행. 이후 `npm run typecheck`, `npm run lint`, `npm run build` 통과 확인.
- 독립 AI 리뷰: 이 단계는 기능 코드가 없어 리뷰 에이전트를 돌리지 않았다. board-layout부터 적용.

---

## [board-layout] 단계별 컬럼 레이아웃

모델: 구현은 Claude Fable 5.1, 리뷰·검증 이후는 Claude Opus 5 (사이클 중간에 사용자가 모델 전환)

### 프롬프트 1
> 매 싸이클에 코드리뷰도 꼼꼼하게 들어간? 휴먼 인루프는 언제 타는거지?

### 프롬프트 2
> 서브에이전트 돌리지 말고 그냥 지금 현 세션에서 하나씩 진행하자

→ 스킬의 리뷰 단계를 "독립 서브에이전트"에서 "현 세션 자가 리뷰(5개 관점 체크리스트)"로 변경.
기록에도 독립 리뷰였다고 쓰지 않고 자가 리뷰임을 명시하기로 함.

### 프롬프트 3 — 가정·설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 불합격 컬럼 배치: 5번째 동일 컬럼 (대안: 시각 구분된 종착 컬럼 / 최종합격과 한 컬럼에 묶기)
- 레이아웃: Flex 가로 스크롤 + 컬럼 고정 최소폭 (대안: CSS Grid 5등분, 좁으면 세로 스택)
- 폴더 구조: 기능별 `features/` + 공유 `shared/` (대안: 종류별 components/hooks/api/types)

### 프롬프트 4
> 앞으로 요구사항 구현 단계는 opus5로 진행해도 될거 같은데

### 프롬프트 5
> 그래 board-layout은 지금 모델로 끝내자, 리뷰 이어가

### 프롬프트 6 — 리뷰 제안 중 채택 선택
1·2·3·5번 채택, 4번(컬럼 본문 키보드 스크롤)은 a11y-keyboard 단계로 이월.

### 프롬프트 7
> 단순한 padding으로 되돌리기

### 프롬프트 8
> 나도 로컬 화면에서 확인하고 싶어

### 프롬프트 9
> 근데 작업하기전에 깃허브에 이슈를 먼저 쫘악 등록하고 진행하면 더 좋을거 같은데?

→ 남은 기능 전체를 GitHub 이슈로 등록(#1~#13). 각 이슈에 원문 요구사항 인용, 수용 기준 체크박스,
결정·가정 포인트, 예상 시간을 넣고 Must/Should 라벨을 붙였다. 이후 기능 커밋은 해당 이슈를 닫는다.
요구사항을 기능 단위로 분해한 흔적이 커밋 이전 단계부터 남는다.

### AI 출력 요지
- 구현: `shared/stages.ts`에 단계 5개를 `as const` 배열로 정의하고 `Stage` 타입을 파생.
  `features/board/Board.tsx`가 그 배열을 순회해 `Column`을 렌더. `Column`은 `<section aria-labelledby>` +
  `<h2>` + 카운트 배지 + 카드가 들어갈 본문 영역. 스타일은 CSS Modules.
- Vite 템플릿의 데모 화면·로고·CSS는 미사용 자산이라 별도 `chore` 커밋으로 먼저 삭제.
- 리뷰 제안 5건: 가로 스크롤 오른쪽 여백[문제], 미사용 타입 `StageId`[제안],
  CSS 중복·구식 문법[제안], 컬럼 본문 키보드 스크롤[제안], `100vh`→`100dvh`[제안].

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트: 버그·엣지 / 요구 누락 / 구조 / 접근성 / 미사용 코드).
  구현은 Fable 5.1, 리뷰는 Opus 5가 수행해 최소한의 관점 차이는 있었다.
- 무엇이 문제였나:
  (1) `StageId` 타입을 정의만 하고 쓰지 않았다. 타입 export라 `noUnusedLocals`에 걸리지 않고 조용히 남는다.
  (2) 컬럼에 `flex: 0 0 280px`와 `min-width: 280px`를 함께 걸었다. flex-shrink가 0이라 후자는 효과가 없다.
  (3) 스크린리더 전용 클래스에 구식 `clip: rect(0,0,0,0)`을 썼다.
  (4) 앱 높이가 `100vh`라 모바일 Safari 주소창만큼 잘린다.
  (5) 컬럼 본문이 세로 스크롤 가능한데 포커스를 받지 못해 키보드로 스크롤할 수 없다 (WCAG 2.1.1).
- 어떻게 알아챘나: 구현 기억이 아니라 `git diff`를 처음부터 다시 읽고 5개 관점을 항목별로 통과/미통과 표시.
  (5)는 `overflow-y: auto`에 `tabindex`가 없는 것을 보고 인지.
- 그대로 채택 / 수정 / 기각:
  (1)(2)(3)(4) 채택해 수정. (5)는 카드가 포커스 가능해지면 자연히 해결될 수 있어 a11y-keyboard 단계로 이월
  (사용자 판단, DECISIONS.md 기록).
- **기각한 내 지적 1건**: "가로 스크롤 컨테이너의 오른쪽 패딩이 스크롤 끝에서 무시된다(WebKit/Blink)"고 지적하고
  CSS 변수 2개 + `::after` 스페이서로 우회 코드를 넣었다. 그러나 **추측으로 기록할 수 없어 실제로 측정했다.**
  Chrome DevTools Protocol로 스크롤을 끝까지 이동시킨 뒤 보드 오른쪽 끝과 마지막 컬럼 오른쪽 끝의 거리를 쟀다.

  | 방식 | padding-right | ::after | 스크롤 끝 여백 |
  |---|---|---|---|
  | 스페이서 (내 수정안) | 0px | 있음 | 24px |
  | padding (원래 코드) | 24px | 없음 | 24px |

  Chrome 154.0.8037.57에서 **재현되지 않았다.** 원래의 단순한 `padding-right`도 정확히 24px가 나온다.
  오래된 브라우저 동작을 근거로 한 잘못된 지적이었다. 재현되지 않는 버그의 우회 코드는 불필요한 복잡도이므로
  사용자 판단으로 원래 방식으로 되돌렸다. Safari(WebKit)는 헤드리스 자동화에 사용자 설정이 필요해 확인하지 못했다.
- 직접 확인한 엣지케이스:
  - 헤드리스 Chrome 1400×900 / 900×700 캡처로 컬럼 5개와 순서(서류검토·면접·처우협의·최종합격·불합격) 확인.
  - 900px에서 `scrollWidth` 1512 / `clientWidth` 900으로 가로 스크롤 발생 확인.
  - 스크롤 최대 지점에서 좌우 여백이 24px로 동일함을 수치로 확인.
  - `npm run typecheck` / `npm run lint` / `npm run build` 통과.

---

## [mock-api] 지연·실패·persist를 갖춘 Mock API

모델: Claude Opus 5

### 프롬프트 1
> mock-api 진행하자

### 프롬프트 2 — 가정·설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 15% 실패 적용 범위: **읽기·쓰기 모두** (대안: 쓰기에만)
- 실패율 조절 수단: **두지 않음** (대안: URL 쿼리파라미터로 조절)
- persist 방식: **모듈 메모리를 서버로 보고 쓰기 성공 시 저장** (대안: 매 호출마다 localStorage 읽고 쓰기)
- 시드 단계 분포: **앞단이 두꺼운 분포** (대안: 5개 컬럼 균등)

### 프롬프트 3 — 리뷰 제안 채택
미사용 export 5개 제거, 저장소 복원 시 필수 필드 샘플 검사 추가. 둘 다 채택.

### AI 출력 요지
- `src/api/mock.ts` — `getApplicants` / `moveStage`. 두 함수 모두 `simulateNetwork()` 관문을 지난다.
  지연은 `200 + Math.random() * 600`, 실패는 `Math.random() < 0.15`에서 `MockApiError`를 던진다.
  모듈 메모리를 서버로 두고 쓰기가 성공했을 때만 localStorage에 직렬화한다.
- `src/api/seed.ts` — mulberry32 의사난수 + 고정 seed(20260923) + 고정 기준일로 1,000건 생성.
  `Math.random()`이나 `new Date()`를 쓰지 않아 몇 번을 돌려도 같은 데이터가 나온다.
- `src/shared/applicant.ts` — 지원자 모델. 원문이 요구한 4개 필드와 가정으로 추가한 4개 필드를 주석으로 구분.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트). 접근성은 UI가 없어 해당 없음.
- 무엇이 문제였나:
  (1) `SEED` `SEED_COUNT` `MIN_DELAY_MS` `MAX_DELAY_MS` `FAILURE_RATE` 5개를 export 했으나 자기 파일
      안에서만 쓰인다. **지난 사이클에 `StageId`를 바로 이 이유로 지웠는데 같은 패턴을 다시 만들었다.**
      일관성이 깨지면 "모순되는 패턴 혼재"로 감점된다.
  (2) localStorage 복원 시 배열이고 비어 있지 않은지만 확인해, 모델이 바뀌면 옛 데이터가 조용히 살아남는다.
- 어떻게 알아챘나: `git diff`를 다시 읽으면서 심볼별로 `grep`을 돌려 파일 밖 사용처가 있는지 실제로 셌다.
  타입 export는 `noUnusedLocals`에 걸리지 않아 도구로는 잡히지 않는다.
- 채택 / 수정 / 기각: 둘 다 채택. export 키워드만 제거해 모듈 내부 상수로 남겼고(수용 기준 "상수로 드러남"은
  그대로 충족), 복원 시 첫 항목의 `id`·`name`·`stage`만 검사하는 함수를 추가했다. 1,000건 전수 검사는 과하다고 판단.
- **내 검증 절차 자체에서 발견한 오류**: 실패한 이동이 저장되지 않는지 확인하려고 "60회 이동을 시도하다 첫 실패에서
  멈추고 상태를 본다"는 테스트를 짰다. 결과가 `hired`로 나왔는데, 실패 전에 이미 성공한 이동이 여러 번 있어서
  **아무것도 증명하지 못하는 테스트**였다. 기대 상태를 로컬에서 추적하며 성공할 때마다 갱신하고 실패한 순간에만
  비교하도록 다시 짰다. 재작성한 테스트에서 7번째 시도에 실패가 났고, 저장된 값이 실패 직전 상태 그대로임을 확인했다.
- 직접 확인한 엣지케이스 (Vite가 서빙하는 모듈을 브라우저에서 직접 호출해 측정):

  | 항목 | 측정값 | 판정 |
  |---|---|---|
  | 지연 범위 (40회) | 최소 210ms · 최대 797ms · 평균 473ms | 200~800ms 충족 |
  | 실패율 (500회) | 65회 실패 = 13.0% | 약 15% 충족 (표준편차 1.6%p 내) |
  | 실패 종류 | MockApiError 외 0건 | 의도한 에러만 발생 |
  | 시드 건수·유일성 | 1,000건 · 고유 id 1,000개 | 충족 |
  | 시드 재현성 | 두 번 생성한 결과 JSON 동일 | 충족 |
  | 단계 분포 | 434 / 236 / 118 / 86 / 126 | 의도한 45·25·10·8·12 비율에 근접 |
  | 지원일 범위 | 2026-03-28 ~ 2026-09-23 | 미래 날짜 없음 |
  | 실패한 이동 | 시도 hired, 저장된 값 screening | 저장되지 않음 |
  | 성공한 이동 | 대상 컬럼의 마지막 인덱스에 위치 | 가정대로 동작 |
  | 새로고침 후 | 이동 결과·마지막 위치 모두 유지, 총 1,000건 | persist 충족 |

- `npm run typecheck` / `npm run lint` / `npm run build` 통과.

---

## [card-list] 지원자 카드 목록

모델: Claude Opus 5

### 프롬프트 1
> card-list 진행하자

### 프롬프트 2 — 범위·설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 로딩·에러 UI 경계: **최소 표시만, #9에서 교체** (대안: 여기서 세 상태 다 구현)
- 컬럼별 분류: **useMemo로 한 번에 그룹화** (대안: 컬럼마다 filter)
- 카드의 현재 단계 표시: **배지로 명시** (대안: 스크린리더용으로만)
- 지원일 형식: **2026. 03. 28. 한국식** (대안: 상대 시간 / ISO 그대로)

### 프롬프트 3 — 리뷰 제안 채택
저장소 경계에서 단계 값 검증, `LoadStatus` export 제거 + 도구로 고정. 둘 다 채택.

### 프롬프트 4 — knip 검증 결과를 보고 판단
> 제거하고 리뷰 체크리스트로

### AI 출력 요지
- `useApplicants` 훅이 조회 한 번을 맡고 `loading` / `ready` / `error` 세 상태를 준다.
  언마운트 뒤 도착한 응답으로 상태를 바꾸지 않도록 취소 플래그를 둔다.
- `Board`가 `useMemo`로 1,000건을 한 번만 순회해 단계별 Map으로 나누고 각 `Column`에 넘긴다.
- `ApplicantCard`는 이름·직무·지원일·현재 단계를 표시한다. 지원일은 문자열을 다시 조합해
  실행 환경 시간대의 영향을 받지 않게 했다.
- 컬럼 본문을 `div`에서 `ul`로 바꾸고 카드를 `li`로 만들었다. 카드마다 제목 태그를 두면
  스크린리더에 제목이 1,000개 생기므로, 목록 의미를 주는 쪽을 택했다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트).
- 무엇이 문제였나:
  (1) 그룹화에서 `groups.get(stage)?.push(...)`를 쓰는데 저장소 검증이 `stage`가 문자열인지만 봤다.
      단계 목록에 없는 값이 저장돼 있으면 그 카드는 **어느 컬럼에도 들어가지 못하고 인원 수에도 안 잡힌 채
      아무 표시 없이 사라진다.**
  (2) `LoadStatus`를 export 했으나 파일 밖에서 쓰이지 않는다. **같은 실수가 세 번째다**
      (StageId → mock API 상수 5개 → LoadStatus).
  (3) 컬럼 `ul`에 `role="list"`가 없다. `list-style: none`을 주면 Safari VoiceOver가 목록 의미를 없애
      "434개 항목"이라는 정보가 사라진다.
- 어떻게 알아챘나: `git diff`를 다시 읽고 export마다 `grep`으로 파일 밖 사용처를 셌다. (1)은 그룹화 코드의
  옵셔널 체이닝이 실패했을 때 무슨 일이 일어나는지 따라가다 발견했다.
- 채택 / 수정 / 기각: 셋 다 반영. (1)은 화면 코드가 아니라 **저장소 경계**에서 막았다. mock API가 복원할 때
  `stage`가 실제 단계 id인지 확인하고 아니면 시드로 재생성한다. 원인을 입구에서 막는 쪽이 낫다고 판단.
  (3)은 속성 하나 추가라 별도로 묻지 않고 반영했다.
- **채택했다가 검증 후 기각한 것 — knip 도입**: (2)가 세 번 반복됐으니 도구로 고정하자고 판단해 knip을 설치했다.
  그런데 실제로 돌려보니 knip은 **"자기 파일 안에서 참조되는 export"를 사용된 것으로 본다.**

  | 내가 만든 미사용 export | knip 탐지 |
  |---|---|
  | `StageId` (자기 파일에서도 미사용) | 잡음 |
  | mock API 상수 5개 (자기 파일에서 사용) | 못 잡음 |
  | `LoadStatus` (자기 파일에서 사용) | 못 잡음 |

  `ignoreExportsUsedInFile`을 `false`로도, 객체 형태로도 설정해 봤지만 결과가 같았다. 별도 프로브 타입을
  넣어 knip이 타입 export 자체는 탐지한다는 것은 확인했으므로, 설정 실수가 아니라 도구의 판정 기준 문제다.
  반대로 곧 쓸 `moveStage`·`MockApiError`를 미사용으로 잡아 오탐이 났다. **세 건 중 한 건만 잡고 오탐 두 건을
  내는 도구에 의존성을 추가할 이유가 없다고 보고 제거했다.** 세 건 모두 실제로는 리뷰 단계의 grep으로 잡았으므로,
  규칙을 리뷰 체크리스트에 명시하는 쪽을 택했다.
- 직접 확인한 엣지케이스 (헤드리스 Chrome 1400×900):

  | 항목 | 측정값 | 판정 |
  |---|---|---|
  | 렌더된 카드 수 | 1,000건 | 충족 |
  | 컬럼 헤더 인원 수 vs 실제 카드 수 | 5개 컬럼 모두 일치 | 충족 |
  | 단계별 분포 | 432 / 236 / 119 / 87 / 126 | 앞선 이동 2건이 반영된 값 (persist 재확인) |
  | 카드 표시 내용 | "전건우 / 데이터 엔지니어 / 2026. 09. 04. / 서류검토" | 요구한 4개 필드 모두 |
  | 지원일 마크업 | `time[datetime]` 존재 | 기계 판독 가능 |
  | 목록 의미 | `ul[role=list]` | Safari 대비 완료 |
  | DOM 노드 수 | 6,052개 | 1,000 카드 기준 |
  | 긴 작업(long task) | 0건 | 50ms 넘는 블로킹 없음 |
  | 강제 레이아웃 | 0.1ms | 문제 없음 |
  | 컬럼 스크롤 20회 | 0.1ms | #13 가상 스크롤 판단의 기준선 |

  현재 수치로는 1,000건에서도 블로킹이 없다. 가상 스크롤(#13) 도입 여부는 검색·필터(#7)를 올린 뒤
  다시 측정해서 판단한다.
- `npm run typecheck` / `npm run lint` / `npm run build` 통과.

---

## [stage-move] 액션 버튼으로 단계 이동

모델: Claude Opus 5

### 프롬프트 1
> stage-move 진행하자

### 프롬프트 2 — 범위·설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- #6과의 경계: **응답 대기 방식, #6에서 낙관적으로 전환** (대안: 바로 낙관적으로)
- 버튼 UI: **카드마다 ← → 버튼 2개** (대안: 단계 선택 드롭다운 / 호버 시에만 표시)
- 이동 후 포커스: **이동한 카드의 같은 버튼으로 옮김** (대안: #10으로 미룸)
- 피드백 범위: **버튼 비활성 + 실패 알림 최소** (대안: 비활성화만)

### 프롬프트 3 — 리뷰 제안 채택
> 핸들러가 직접 되찾기

이동 중 안내는 #6으로 미룸.

### 프롬프트 4 — 측정 결과를 보고 판단
> 제거

### AI 출력 요지
- `adjacentStage(from, direction)`가 한 단계 옆을 찾고 경계에서는 `null`을 준다. 화살표 키 가정과
  버튼이 같은 함수를 쓴다.
- 훅에 `moveApplicant`를 더했다. 응답을 기다렸다가 배열에서 빼서 끝으로 밀어 넣는다.
  서버가 같은 순서를 만들므로 카드가 대상 컬럼의 마지막에 놓인다.
- 카드에 ← → 버튼. 경계와 이동 중에는 비활성. `aria-label`로 "이름: 대상단계 단계로 이동"을 읽어준다.
- 보드가 이동 후 포커스를 되돌린다. `data-move="<id>:<방향>"` 속성으로 새 위치의 버튼을 찾는다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트). export는 이번에 새로 만든 2개 모두 파일 밖에서
  쓰이는 것을 grep으로 확인했다 (지난 세 번의 반복을 막기 위해 체크리스트에 넣어둔 항목).
- 무엇이 문제였나:
  (1) 포커스 대상을 `useRef` 하나에 담고 목록이 바뀔 때 복원하게 짰다. **이동 두 건이 겹치면 ref가 덮여
      아직 움직이지도 않은 카드로 포커스가 간다.** 두 번째 이동이 끝날 때는 ref가 비어 아무 일도 안 일어난다.
  (2) 이동을 시작하면 두 버튼이 비활성이 되는데, 브라우저는 포커스된 요소가 비활성화되면 포커스를 body로
      보낸다. 성공하면 목록이 바뀌어 복원되지만 **실패하면 목록이 그대로라 복원 효과가 돌지 않아 포커스가
      body에 갇힌다.** 15% 확률로 키보드 사용자가 처음부터 다시 탭해야 한다.
  (3) 이동 중이라는 사실이 스크린리더에 전달되지 않는다.
- 어떻게 알아챘나: 포커스 복원을 "목록 변경"에 의존시킨 설계를 다시 읽으면서, 목록이 바뀌지 않는 경로
  (실패)와 두 번 바뀌는 경로(동시 이동)를 각각 따라갔다. (2)는 이후 브라우저 측정에서 이동 중
  `document.activeElement`가 실제로 `BODY`임을 확인해 뒷받침했다.
- 채택 / 수정 / 기각: (1)(2)는 원인이 같아 한 번에 고쳤다. ref와 목록 의존 효과를 없애고, 각 클릭 핸들러가
  `await` 뒤 `requestAnimationFrame`에서 자기 카드의 버튼을 직접 되찾는다. 호출마다 별도 클로저라 겹쳐도
  섞이지 않고 성공·실패 모두 같은 경로를 탄다. (3)은 낙관적 업데이트로 바꾸면 "이동 중" 자체가 사라지므로
  #6으로 미뤘다 (사용자 판단).
- **측정에서 새로 나온 것 — 화살표를 감싼 `span` 제거**: 버튼을 붙이자 DOM이 6,052에서 10,053노드로 늘었다.
  버튼 2,000개 외에 `aria-hidden` `span`이 2,000개 붙은 탓이다. 이 `span`이 정말 필요한지 추측하지 않고
  Chrome 접근성 트리에서 버튼의 접근 가능한 이름과 그 출처를 직접 읽었다.

  | 마크업 | 접근 가능한 이름 | 이름 출처 | DOM 노드 |
  |---|---|---|---|
  | `<button aria-label><span aria-hidden>←</span></button>` | 임준서: 면접 단계로 이동 | attribute | 10,053 |
  | `<button aria-label>←</button>` | 임준서: 면접 단계로 이동 | attribute (contents보다 우선) | 8,053 |

  이름이 `aria-label`에서만 나오므로 `span`은 접근성에 기여하지 않는다. 제거해 2,000노드를 줄였다.
- 직접 확인한 엣지케이스 (헤드리스 Chrome, 클릭은 실제 DOM 이벤트로):

  | 항목 | 측정값 | 판정 |
  |---|---|---|
  | 이동 동작 | applicant-0008이 서류검토 → 면접 | 충족 |
  | 이동 중 포커스 | `BODY` | 리뷰 (2)번이 실제로 재현됨 |
  | 이동 후 포커스 | `applicant-0008:next` | 복원됨 |
  | 실패 시 포커스 | 3번째 시도에 실패, `applicant-0009:next` | 실패해도 복원됨 |
  | 실패 알림 | "단계를 이동하지 못했습니다. 다시 시도해 주세요." | 표시됨 |
  | 첫 컬럼 ← / → | 비활성 / 활성 | 경계 가정대로 |
  | 마지막 컬럼 → / ← | 비활성 / 활성 | 경계 가정대로 |
  | 버튼 수 | 2,000개 (카드 1,000 × 2) | — |
  | DOM 노드 | 8,053개 | span 제거 후 |

- `npm run typecheck` / `npm run lint` / `npm run build` 통과.

---

## [optimistic-update] 낙관적 업데이트 · 롤백 · 경쟁 상태

모델: Claude Opus 5

### 프롬프트 1
> optimistic-update 진행해줘

### 프롬프트 2 — 설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 롤백 전략: **카드별 서버 확정 단계 추적** (대안: 이동 직전 전체 목록 snapshot)
- 경쟁 상태: **카드별 요청 시퀀스, 최신만 반영** (대안: 카드별 잠금 / 시퀀스 + 저장 중 표시)
- 롤백 위치: **원래 단계의 맨 아래** (대안: 원래 인덱스까지 복원)
- 실패 피드백: **토스트** (대안: 기존 알림 줄 유지)

### 프롬프트 3 — 재현 결과를 보고 판단
> 카드별 요청 직렬화

### AI 출력 요지
- 클릭 즉시 목록을 바꾸고 API를 부른다. 실패하면 그 카드만 되돌린다.
- 롤백 기준은 카드별 `confirmed` 맵이다. 서버가 마지막으로 확정해 준 지원자를 들고 있다가
  실패 시 그 단계로 돌린다.
- 카드별 요청 시퀀스로 늦게 온 응답을 버린다.
- 이동 중 버튼 잠금을 없앴다. 낙관적 반영 뒤에도 못 누르게 하면 낙관적 UI의 이점이 사라진다.
- 실패는 토스트로 알린다. 5초 뒤 사라지고 최대 4개까지 쌓인다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트).
- 무엇이 문제였나: **요청 시퀀스만으로는 화면과 서버가 갈린다.** mock API가 호출마다 200~800ms
  난수 지연을 주므로 나중에 보낸 요청이 먼저 도착할 수 있다. 서버의 최종 쓰기는 마지막에 *도착한*
  요청이 정하는데 화면은 마지막에 *보낸* 요청을 따른다. 수용 기준의 "늦게 도착한 응답이 최신 상태를
  덮어쓰지 않음"은 충족하지만 "데이터가 깨지지 않아야 함"은 못 지킨다. 새로고침하면 값이 바뀐다.
- 어떻게 알아챘나: 시퀀스 검사를 다시 읽으면서 "응답을 버리면 서버 쓰기도 없던 일이 되는가"를 따라갔다.
  mock API는 지연 뒤에 쓰므로 응답을 버려도 쓰기는 이미 일어난 뒤였다. **추측으로 기록하지 않고
  실제로 재현했다.** 같은 카드를 30ms 간격으로 두 번 옮긴 뒤 화면 컬럼과 localStorage 값을 비교했다.

  | | 시행 | 불일치 | 예시 |
  |---|---|---|---|
  | 수정 전 (시퀀스만) | 12회 | **7회** | applicant-0010 화면=처우협의 / 서버=면접 |
  | 수정 후 (직렬화 추가) | 12회 | **0회** | — |

- 채택 / 수정 / 기각: 카드별 요청 직렬화를 채택했다. 같은 카드의 다음 네트워크 호출을 앞 호출이 끝난
  뒤에 보낸다. 서버 쓰기 순서가 사용자 의도 순서와 같아져 불일치가 사라진다. **줄을 서는 것은 네트워크
  호출이지 화면이 아니다.** 화면은 여전히 클릭 즉시 바뀐다 (측정값 49~57ms).
  시퀀스 검사는 그대로 두었다. 직렬화가 순서를 보장하고, 시퀀스는 화면이 사용자의 마지막 의도를
  지키게 한다. 둘은 서로 다른 일을 한다.
  함께 고친 것: 성공 응답은 최신 요청이 아니어도 `confirmed`를 갱신한다. 서버가 실제로 썼기 때문이다.
  이것을 빠뜨리면 "#1 성공 + #2 실패"에서 롤백 목적지가 틀린다.
- 기각한 대안: 응답 후 서버 값으로 화면을 강제 동기화하는 방법도 있었지만, 사용자가 마지막으로 누른
  결과가 되돌려지는 것이라 수용 기준 5번과 정면으로 충돌한다 (사용자 판단).
- 직접 확인한 엣지케이스:

  | 수용 기준 | 측정값 | 판정 |
  |---|---|---|
  | 이동 즉시 UI 반영 | 클릭 → 컬럼 변경까지 49·51·53·56·56·57ms (API 지연 최소 200ms) | 충족 |
  | API 실패 시 원상 복구 | applicant-0075: 서류검토 → 면접(낙관적) → 서류검토(롤백) | 충족 |
  | 실패 피드백 | 토스트 "조태윤: 이동하지 못해 서류검토 단계로 되돌렸습니다." | 충족 |
  | 연속 이동 후 최종 상태 | 화면과 저장값 12회 모두 일치 (수정 전 7회 불일치) | 충족 |
  | 늦은 응답이 덮어쓰지 않음 | 시퀀스 검사로 stale 응답 폐기 | 충족 |
  | 토스트 알림 영역 | `role="status"` (polite) | 15% 실패에서 읽던 내용을 끊지 않음 |

- `npm run typecheck` / `npm run lint` / `npm run build` 통과.

---

## [stage-merge] 최종합격·불합격을 결과 한 컬럼으로 통합 + 칩 토글

모델: Claude Opus 5

### 프롬프트 1
> 최종합격이랑 불합격은 컬럼 1개로 합치는게 나을거 같아.. 이게 좀더 단계도 명확해지고 좋을거 같고..
> 합격인지 불합격인지는 해당 칩을 클릭하면 토글로 바뀌는 형태로 가면 좋을거 같은데?

### 프롬프트 2
> 이건 내 임시 가정으로 잡아주면 좋을거 같아

### 프롬프트 3
> 이슈 14번 작업 진행해줘

### 프롬프트 4 — 설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 데이터 모델: **단계 5개 유지 + 컬럼 4개 매핑** (대안: result 단계 + outcome 필드로 변경)
- 칩 버튼 범위: **결과 컬럼에서만 버튼** (대안: 모든 컬럼에서 버튼, 다른 곳은 비활성)
- 결과 컬럼 헤더: **합계만** (대안: 합격 N · 불합격 M 분리)
- 시각 구분: **칩 색 + 글자** (대안: 카드 테두리 색까지 / 칩 글자만)

### 프롬프트 5 — 리뷰 제안 판단
> 그대로 두고 가정으로 명시

### AI 출력 요지
- `STAGES`(저장되는 5단계)와 `COLUMNS`(화면의 4컬럼)를 분리했다. 결과 컬럼이 `hired`·`rejected`
  두 단계를 담는다. mock API·시드·이미 저장된 데이터는 건드리지 않았다.
- 이동이 단계 단위에서 **컬럼 단위**로 바뀌었다. `adjacentStage`가 컬럼 인덱스를 걸어 옆 컬럼의
  첫 단계를 돌려준다. 결과 컬럼의 첫 단계가 `hired`라 처우협의에서 `→`는 최종합격으로 들어간다.
- 결과 컬럼 카드의 칩만 버튼이 된다. `toggledResultStage`가 null이면 읽기 전용 텍스트로 남는다.
- 토글은 훅을 고치지 않고 기존 `moveApplicant`를 그대로 탄다. 낙관적 반영·롤백·요청 직렬화가
  자동으로 따라온다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트).
- 무엇이 문제였나:
  (1) `Stage` 타입이 아무 데서도 쓰이지 않게 되었다. `Column` 컴포넌트가 `BoardColumn`을 쓰게 되면서
      마지막 사용처가 사라졌다. **미사용 export 문제의 네 번째지만 성격이 다르다.** 앞의 셋은 내가 새로
      만든 것이었고 이번엔 리팩터링으로 쓰임이 없어진 것이다. "새로 만들 때 확인한다"는 체크리스트만으로는
      잡히지 않는다.
  (2) 토글하면 카드가 결과 컬럼 맨 아래로 밀린다. 200장 넘는 컬럼에서 방금 토글한 카드가 화면 밖으로 나간다.
  (3) `toggledResultStage`에서 값을 좁은 타입으로 캐스팅했다. 배열을 캐스팅하는 편이 안전하다.
- 어떻게 알아챘나: stages.ts의 export를 하나씩 `grep`으로 세는 도중 `Stage`만 0곳이 나왔다. (2)는
  `applyMove`가 항상 배열 끝으로 보낸다는 것을 알고 토글 경로에 대입해 보고 알았다.
- 채택 / 수정 / 기각: (1)(3)은 수정했다. (2)는 **그대로 두기로 했다**(사용자 판단). 서버도 같은 순서를
  만들어 새로고침 후에도 일치하고, "방금 움직인 카드가 맨 아래"라는 기존 규칙과 같다. 제자리에 두려면
  화면과 서버가 어긋나거나 완료된 #3의 API 표면을 다시 손대야 한다. 알고 남긴 것이므로 가정으로 적었다.
- 놓칠 뻔한 레이아웃 문제: 칩이 결과 컬럼에서만 `button`이 되면서, 이동 버튼을 오른쪽으로 미는
  `.move:first-of-type { margin-left: auto }` 규칙이 결과 컬럼에서만 깨진다. `:first-of-type`이 요소 종류를
  보기 때문에 칩이 첫 번째 button이 되어버린다. 칩에 `margin-right: auto`를 주는 방식으로 바꿔
  칩이 span이든 button이든 같은 레이아웃이 나오게 했다.
- 직접 확인한 엣지케이스 (헤드리스 Chrome 1400×900):

  | 수용 기준 | 측정값 | 판정 |
  |---|---|---|
  | 컬럼 4개 | 서류검토 · 면접 · 처우협의 · 결과 | 충족 |
  | 결과 컬럼에 두 단계가 함께 | 칩 종류 "최종합격", "불합격" 2가지 | 충족 |
  | 시각적 구분 | 최종합격 녹색 / 불합격 회색, 글자도 그대로 | 색에만 의존하지 않음 |
  | 헤더 인원 수 | 214 = 실제 카드 214장 | 합계로 일치 |
  | 다른 컬럼의 칩 | 토글 버튼 0개 | 결과 컬럼에서만 버튼 |
  | 칩 토글 | 불합격 → 최종합격, 저장값 rejected → hired | 충족 |
  | 토글의 낙관적 반영 | 클릭 50ms 뒤 칩이 이미 바뀜 | 충족 |
  | 토글 실패 시 롤백 | applicant-0207: 최종합격 → 불합격(낙관적) → 최종합격 | 충족 |
  | 토글 실패 토스트 | "조태윤: 이동하지 못해 최종합격 단계로 되돌렸습니다." | 충족 |
  | 토글 후 포커스 | 칩에 그대로 유지 (성공·실패 모두) | 충족 |
  | 빠른 연속 토글 | 12회 모두 화면 칩과 저장값 일치 | 직렬화가 토글에도 적용됨 |
  | 처우협의 `→` | aria-label "오다은: 최종합격 단계로 이동" | 기본값이 최종합격 |
  | 결과 컬럼 `→` / `←` | 비활성 / "강서연: 처우협의 단계로 이동" | 경계 가정대로 |
  | 칩의 접근 가능한 이름 | "강서연: 현재 최종합격. 눌러서 불합격 단계로 변경" | 현재 상태와 동작을 함께 전달 |
  | DOM 노드 | 8,048개 | 컬럼이 하나 줄어 소폭 감소 |

- `npm run typecheck` / `npm run lint` / `npm run build` 통과.

---

## [toggle-in-place] 결과 토글 시 카드를 제자리에 유지

모델: Claude Opus 5

### 프롬프트 1
> 최종합격을 토글하면 맨 아래로 가는건 별로 인거 같아..
> 그냥 기존 해당 인덱스에 위치시키는게 맞는거 같아

### 프롬프트 2
> 이걸로 인한 사이드 이펙트가 있나?

### 프롬프트 3
> mock API도 업데이트 같이 해주면 좋겠어

### 프롬프트 4 — 리뷰 제안 판단
> 둘 다 필수로

### AI 출력 요지
- **사이드 이펙트 질문에 먼저 답했다.** 롤백 경로를 같이 고쳐야 한다는 점(실패했을 때만 카드가 튀면 더
  이상하다), 되돌리기(#12)에 선택지가 생긴다는 점, 가상 스크롤(#13)에 유리하다는 점,
  `moveStage`라는 이름이 덜 정확해진다는 점을 짚었다. 지연·실패율·경쟁 상태 처리는 영향 없음을 확인했다.
- `moveStage(id, toStage, placement)`에 `'end'` / `'keep'`을 추가했다. 훅의 `applyMove`와 롤백 경로도
  같은 값을 받는다. 컬럼 이동은 `'end'`, 결과 토글은 `'keep'`.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트). `applyMove`·`moveStage` 호출부를 전부 `grep`해
  네 곳 모두 자리 규칙을 넘기는지 확인했다.
- 무엇이 문제였나: 자리 규칙에 `'end'` 기본값을 뒀는데 호출부가 전부 명시적으로 넘겨 **한 번도 쓰이지 않는다.**
  이 프로젝트에서 미사용 코드로 네 번 걸린 터라 그냥 두기 어려웠다.
- 어떻게 알아챘나: 호출부를 세면서 기본값이 발동하는 경로가 없다는 것을 확인했다.
- 채택 / 수정 / 기각: 채택. 두 함수 모두 필수 인자로 바꿨다. 새 호출부가 생길 때 자리를 반드시 고르게 해
  조용히 `'end'`가 적용되는 사고를 막는다.
- **화면만 고치면 안 되는 이유를 사용자에게 먼저 설명했다**: 화면에서만 자리를 지키면 서버는 여전히 끝으로
  보내 새로고침했을 때 카드가 튄다. 문제가 없어지는 게 아니라 새로고침까지 미뤄질 뿐이다. 화면과 서버가
  갈리는 것은 #6에서 실측으로 잡아낸 문제라 다시 만들 수 없다고 보고 mock API를 함께 고쳤다.
- 직접 확인한 엣지케이스:

  | 수용 기준 | 측정값 | 판정 |
  |---|---|---|
  | 토글 시 자리 유지 | applicant-0106: 인덱스 5 → 낙관적 5 → 최종 5 | 충족 |
  | 컬럼 이동은 맨 아래 | applicant-0002: 결과 컬럼 215장 중 214번(마지막) | 기존 동작 유지 |
  | 새로고침 후 순서 | 1,000장 전체 순서가 한 장도 어긋나지 않음 | 화면과 저장값 일치 |

  새로고침 비교는 토글 1회와 컬럼 이동 1회를 한 뒤 전체 카드 순서를 대조한 것이다. 여러 카드를 동시에
  움직이는 부하 상황까지 검증한 것은 아니다.
- `npm run typecheck` / `npm run lint` / `npm run build` 통과.

---

## [search-filter] 이름 검색 + 직무 필터

모델: Claude Opus 5

### 프롬프트 1
> search-filter 진행하자

### 프롬프트 2 — 설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 입력 반응: **useMemo + useDeferredValue** (대안: useMemo만 / 수동 디바운스)
- 직무 목록: **불러온 데이터에서 유도** (대안: 상수로 정의)
- 필터 중 컬럼 헤더 인원 수: **필터된 수** (대안: 전체 수 유지 / 둘 다 표기)
- 0건 UI 범위: **보드 전체 0건만 안내** (대안: 컬럼별 빈 상태까지 지금)

### 프롬프트 3 — 리뷰 제안 채택
높이 연결은 래퍼에 항상 클래스 적용, 결과 수는 미룬 값으로 통일. 둘 다 채택.

### AI 출력 요지
- 필터를 데이터 훅과 분리해 `useApplicantFilter`로 뒀다. 검색어·직무·결과 목록만 책임진다.
- **진짜 비용은 필터 계산이 아니라 재조정이라고 보고 `useDeferredValue`를 썼다.** 1,000건을 훑는 것은
  마이크로초 단위다. 키를 누를 때마다 최대 1,000장을 다시 그리는 React 재조정이 병목이다.
  입력란은 즉시 값을, 목록은 미룬 값을 쓴다. 디바운스와 달리 결과가 늦게 나타나지 않고 타이머 관리도 없다.
- 직무 선택지는 불러온 지원자에서 유도한다. 결과가 0건인 것이 확정된 선택지를 만들지 않기 위함이다.
- 목록이 한 박자 뒤처진 동안 보드를 옅게 표시해 멈춘 것이 아님을 알린다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트). 새 export 3개 모두 파일 밖 사용처를 확인했다.
  처음 돌린 `grep`에서 제외 패턴을 잘못 써 `ALL_ROLES`가 0곳으로 나왔는데, 정확히 다시 세어
  `FilterBar`에서 쓰이는 것을 확인했다. 도구 출력을 그대로 믿지 않고 한 번 더 본 것이 맞았다.
- 무엇이 문제였나:
  (1) 보드를 감싼 `div`가 **높이 연결을 끊는다.** 높이 100%를 뒤처진 상태일 때만 붙여, 평소에는
      클래스가 아예 없다. 보드의 `height: 100%`가 기댈 부모 높이가 사라져 컬럼이 내용만큼 늘어나고
      컬럼별 스크롤 대신 페이지 전체가 세로로 스크롤된다.
  (2) 검색 결과 수가 **한 박자 어긋난다.** 표시 여부는 즉시 값으로 판단하는데 숫자는 미룬 값에서 나온다.
      첫 글자를 친 직후 잠깐 "1000명이 검색되었습니다"가 보이고, 스크린리더는 이 틀린 숫자를 읽는다.
- 어떻게 알아챘나: (1)은 `main → div → Board`로 이어지는 높이 사슬을 따라가다 조건부 클래스에서 끊기는
  것을 봤다. (2)는 미룬 값을 쓰기로 해놓고 어느 값이 어디에 쓰이는지 표시해 보다가 `isFiltering`만
  즉시 값으로 남아 있는 것을 발견했다. 성격이 분명한 내 실수다.
- 채택 / 수정 / 기각: 둘 다 수정. (1)은 래퍼에 높이 클래스를 항상 주고 흐림 효과만 수식자로 분리했다.
  (2)는 `isFiltering`도 미룬 값으로 계산해 숫자와 목록이 항상 같은 시점을 가리키게 했다.
- 직접 확인한 엣지케이스 (헤드리스 Chrome 1400×900, 1,000건):

  | 수용 기준 | 측정값 | 판정 |
  |---|---|---|
  | 이름 검색 | "김" → 46명, 렌더된 카드 전부 이름에 "김" 포함 | 충족 |
  | 직무별 필터 | "안드로이드 개발자" → 130명 | 충족 |
  | 검색 + 필터 AND | "김" + 안드로이드 개발자 → 9명, 전부 두 조건 만족 | 충족 |
  | 컬럼 헤더 인원 수 | 4개 컬럼 합계 46 = 렌더된 카드 46장 | 필터된 수로 일치 |
  | 결과 0건 | "조건에 맞는 지원자가 없습니다" 표시, 필터바 유지 | 조건을 되돌릴 수 있음 |
  | 직무 선택지 | 전체 직무 + 실제 존재하는 8개 | 데이터에서 유도됨 |
  | 입력 반응 속도 | 6타 연속 입력에서 45 · 7 · 6 · 6 · 6 · 6ms | 체감 지연 없음 |
  | 긴 작업(long task) | 0건 | 50ms 넘는 블로킹 없음 |
  | 높이 연결 | 보드 높이 = main 높이, 페이지 세로 스크롤 없음 | 수정 확인 |
  | 결과 수 안내 | `role="status"`로 "46명이 검색되었습니다." | 화면을 못 봐도 전달됨 |

  첫 타의 45ms는 필터가 처음 적용되며 1,000장에서 46장으로 줄어드는 비용이다. 이후 6ms대로 떨어진다.
- `npm run typecheck` / `npm run lint` / `npm run build` 통과.

---

## [result-confirm] 처우협의 → 결과 이동 시 합격 여부 확인

모델: Claude Opus 5

### 프롬프트 1
> 처우 협의에서 결과로 넘어갈땐 최종합격인가요? 라는 알럿이 띄워지면 좋겠는데?
> 예 아니요 버튼이 있고, 예를 클릭하면 최종합격으로 보이고,
> 아니요로 하면 불합격으로 보이면 좋겟어. 이것도 내 가정이긴 한데 티켓에 관련 코멘트 추가하고 기능 구현해주면 좋겠어

### 프롬프트 2
> 16번 진행하자

### 프롬프트 3 — 설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 구현 방식: **네이티브 `<dialog>` 요소** (대안: 직접 만든 모달 / 재사용 모달 컴포넌트)
- 기본 포커스: **취소 버튼** (대안: 예 버튼 / 창 자체)
- 적용 범위: **처우협의 → 결과만** (대안: 결과에서 되돌아올 때도)

### AI 출력 요지
- 요청을 받은 시점이 search-filter(#7) 진행 중이라 **가정과 티켓을 먼저 남기고 구현은 분리했다**
  (커밋 f7f4516, 이슈 #16). 한 커밋에 두 기능이 섞이지 않게 하기 위함이다.
- 네이티브 `<dialog>`의 `showModal()`을 쓴다. 포커스 트랩, ESC 닫기, 배경 조작 차단, 최상위 레이어를
  브라우저가 맡는다. 직접 만들면 틀리기 쉬운 부분이다.
- 확인 전에는 카드가 움직이지 않는다. 답을 받은 뒤에야 기존 이동 경로를 탄다.
- `isResultDecision(toStage, direction)`이 확인이 필요한 이동인지 판정한다. 결과 컬럼이 마지막이라
  `next`로 결과 컬럼에 들어오는 경로는 처우협의뿐이다.
- 카드가 지원자 객체를 넘기도록 바꿨다. 보드가 확인 창에 이름을 보여주려면 id만으로는 부족하다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트). 새 export 3개 모두 파일 밖 사용처를 확인했다.
- **검증 절차에서 내 실수를 하나 잡았다.** "확인 창이 지원자 이름을 알려주는가"를 확인한다면서
  `body.includes(cond ? '' : '')` 처럼 **항상 참이 되는 식**을 썼다. 결과가 `true`로 나왔지만 아무것도
  증명하지 못한다. 카드의 이름을 읽어 창 본문에 실제로 들어 있는지 비교하도록 다시 짰고,
  "한수빈" 카드에서 창 본문이 "한수빈 지원자를 결과 단계로 옮깁니다"인 것을 확인했다.
  mock-api 사이클에서도 같은 종류의 실수를 했었다. 통과한 결과를 그대로 적었으면 거짓 기록이 될 뻔했다.
- 직접 확인한 엣지케이스 (헤드리스 Chrome, 실제 DOM 이벤트):

  | 수용 기준 | 측정값 | 판정 |
  |---|---|---|
  | 확인 전 카드 고정 | 창이 열린 상태에서 컬럼 그대로, 저장값도 `offer` 유지 | 충족 |
  | 예 → 최종합격 | 결과 컬럼, 칩 "최종합격", 저장값 `hired` | 충족 |
  | 아니요 → 불합격 | 결과 컬럼, 칩 "불합격", 저장값 `rejected` | 충족 |
  | 취소·ESC | 창 닫힘, 컬럼 그대로, 저장값 `offer` 유지 | 충족 |
  | 취소 후 포커스 | 원래 눌렀던 이동 버튼으로 복귀 | 충족 |
  | 포커스 트랩 | 창 밖 입력란에 `focus()`를 시도해도 포커스가 창 안에 남음 | 충족 |
  | 기본 포커스 | "취소" 버튼 | Enter 연타로 결정이 확정되지 않음 |
  | 접근 가능한 이름 | `aria-labelledby`가 "최종합격인가요?" 제목을 가리킴 | 충족 |
  | 지원자 이름 | "한수빈" 카드 → 본문 "한수빈 지원자를 결과 단계로 옮깁니다" | 충족 |
  | 다른 컬럼 간 이동 | 창이 뜨지 않음 | 충족 |
  | 결과 칩 토글 | 창이 뜨지 않음 | 충족 |
  | 결과 → 처우협의 되돌리기 | 창이 뜨지 않음 | 적용 범위대로 |

- `npm run typecheck` / `npm run lint` / `npm run build` 통과.

---

## [responsive] 모바일 화면 대응

모델: Claude Opus 5

### 프롬프트 1
> 지금 웹화면만 대응하고 있는데 모바일 화면에도 대응하고 싶어

### 프롬프트 2
> 모바일 화면에선 각 컬럼을 맨 상위에 탭을 클릭하면 해당 컬럼으로 스위칭 되도록 하면 좋을거 같구
> 특정 카드를 클릭하고 좌우로 움직이면 맨 상위 선택된 탭도 바뀌고
> 해당 컬럼 내용으로 이동되도록 하면 좋을거 같아
> 검색은 맨 상단에 위치하도록 하면 좋을거 같고

### 프롬프트 3
> 17번 진행하자

### 프롬프트 4 — 설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 탭 마크업: **일반 버튼 + `aria-current`, CSS로 분기** (대안: ARIA 탭 패턴 + JS 조건부 렌더 / 탭 패턴 + CSS 분기)
- 기준 폭: **768px 이하** (대안: 640px / 1024px)
- 터치 영역: **데스크톱 24px, 모바일 44px** (대안: 모바일에서만 확대 / 모든 화면 44px)

### 프롬프트 5 — 리뷰 제안 채택
탭 묶음에 `role="group"` + `aria-label`, 보드 높이를 `flex: 1 1 auto`로 명시. 둘 다 채택.

### AI 출력 요지
- **ARIA 탭 패턴을 쓰지 않기로 했다.** 탭 패턴은 좌우 화살표를 탭 전환에 배정하는데, 이 앱에서는
  좌우 화살표가 카드 이동이다(#10 예정). 일반 버튼 + `aria-current`면 의미가 갈리지 않는다.
- 데스크톱과 모바일이 **같은 마크업**을 쓴다. `role`은 CSS로 바꿀 수 없지만 표시 여부는 바꿀 수 있다.
  컬럼에 `data-active`를 두고 CSS가 보고 정한다. 조건부 렌더링과 `matchMedia` 상태가 필요 없다.
- 카드를 옮기면 보이는 컬럼도 함께 넘어간다. 그러지 않으면 옮긴 카드가 보이지 않는 곳으로 사라진다.
  되돌려지면 보이는 컬럼도 되돌아온다.
- 터치 영역은 이동 버튼 24→44px, 결과 칩 24→44px. 읽기 전용 칩은 키우지 않았다. 누를 수 없는 것에
  터치 기준을 적용할 이유가 없다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트).
- **의심한 두 가지가 모두 재현되지 않았다.** 추측을 기록하지 않으려고 좁은 화면에서 직접 쟀다.
  (1) 탭 막대가 45px를 차지하는데 보드에 `height: 100%`가 걸려 있어 그만큼 넘칠 것이라고 봤다.
      실제로는 넘침이 0이었다. 보드가 flex 자식이라 `flex-shrink`가 줄여준 것이다.
  (2) 360px에서 탭 4개("서류검토"·"면접"·"처우협의"·"결과" + 인원 수)가 넘칠 것이라고 봤다.
      실제로는 `scrollWidth`와 `clientWidth`가 같아 넘치지 않았다.
- 무엇이 문제였나:
  (1) 탭 묶음이 버튼 4개가 든 평범한 `div`라 접근 가능한 이름이 없다. 묶음의 시작과 끝을 알 수 없다.
  (2) 보드 높이가 `height: 100%`로 부모 전체를 잡은 뒤 flex 축소에 기대 맞는다. 결과는 정확하지만
      의도가 코드에 드러나지 않아, 나중에 보드 위아래에 요소가 붙으면 다시 볼 지점이 된다.
- 채택 / 수정 / 기각: 둘 다 채택. `role="group"` + `aria-label="채용 단계 선택"`을 붙이고,
  보드를 `flex: 1 1 auto; min-height: 0`으로 바꿔 의도를 드러냈다. 고친 뒤 다시 재서 수치가 같은 것을 확인했다.
- 직접 확인한 엣지케이스 (Chrome 기기 에뮬레이션):

  | 수용 기준 | 측정값 | 판정 |
  |---|---|---|
  | 390px 탭 표시 | 탭 4개, 폭 99·96·99·96px, 넘침 없음 | 충족 |
  | 360px 탭 표시 | 폭 99·81·99·81px, `scrollWidth` = `clientWidth` | 넘치지 않음 |
  | 한 컬럼만 표시 | 보이는 컬럼 1개, 숨은 컬럼 3개는 높이 0 | 충족 |
  | 탭 클릭 전환 | 결과 탭 클릭 → 보이는 컬럼 "결과" | 충족 |
  | 이동 시 탭 자동 전환 | 면접에서 `→` → 클릭 120ms 뒤 탭이 이미 "처우협의" | 충족 |
  | 이동 후 카드 추적 | 카드가 계속 보이고 포커스도 그 카드에 유지 | 충족 |
  | 숨은 컬럼의 포커스 | 버튼 1,414개에 `focus()`를 시도해도 `activeElement`가 `BODY` | 탭 순회에 끼지 않음 |
  | 이동 버튼 터치 영역 | 모바일 44×44px, 데스크톱 24×24px | WCAG 2.5.8 충족 |
  | 결과 칩 터치 영역 | 모바일 60×44px | 충족 |
  | 확인 창 | 폭 358px로 390px 화면에 들어감, 버튼 3개 44px 세로 배치 | 충족 |
  | 검색 위치 | 검색(103px) → 탭(162px), 검색이 탭 위 | 충족 |
  | 세로·가로 스크롤 | 둘 다 없음 | 충족 |
  | 데스크톱 유지 | 탭 `display: none`, 컬럼 4개 모두 표시, 버튼 24px | 기존 레이아웃 그대로 |

- **측정에서 나온 숫자 하나를 남겨 둔다**: 좁은 화면에서 숨겨진 컬럼 3개에 버튼이 1,414개,
  전체 DOM이 8,095노드다. 포커스는 받지 못하지만 DOM에는 남는다. 가상 스크롤(#13) 판단의 근거가 된다.
- `npm run typecheck` / `npm run lint` / `npm run build` 통과.

---

## [rollback-test] 롤백·경쟁 상태 테스트

모델: Claude Opus 5.5 (1M context)

> 표기 정정: 커밋 c9deba9의 Co-Authored-By는 작업 지시문에 적힌 대로 "Claude Opus 5 (1M context)"로 나갔다. 실제 모델 ID는 `claude-opus-5-5`다. push한 커밋은 고치지 않는다는 규칙(amend 금지)에 따라 그 커밋은 그대로 두고, 이 기록과 이후 커밋을 Opus 5.5로 통일한다.

병렬 워크트리(`feat/rollback-test`, main 51b88b4 기준)에서 진행했다.

### 프롬프트 1
> 이 워크트리는 recruitment-pipeline-board 과제의 병렬 브랜치다. main은 커밋 51b88b4까지 동기화되어 있다.
> (규칙 1~6: /feature-step 사이클 준수, node PATH, 포트 5176, 기록 문서 평소대로, 커밋 규칙, 다른 워크트리 금지)
>
> 담당: 이슈 #11 [rollback-test] 롤백·경쟁 상태 테스트. 포트 5176.
> 새 테스트 파일과 vitest 설정, package.json 만 건드려라. 기존 소스는 되도록 바꾸지 마라.
> gh issue view 11 의 코멘트에 테스트 대상 6개가 구체화되어 있다. 특히 카드별 요청 직렬화와 카드별 확정 상태 롤백이 핵심이다.

### 프롬프트 2 — 가정·설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- `applyMove` 검증: **`export` 한 줄 추가** (대안: 소스 무변경, 훅으로만 간접 검증)
- mock 제어: **`vi.mock` + 수동 deferred** (대안: 실제 mock + 가짜 타이머·`Math.random` 고정 / 둘 다)
- DOM 환경: **jsdom** (대안: happy-dom)
- 설정 위치: **별도 `vitest.config.ts`** (대안: `vite.config.ts`에 test 블록)

### 프롬프트 3 — 리뷰 제안 채택
리뷰 1·2·3번 모두 채택.

### AI 출력 요지
- `moveStage`를 대체해 호출마다 대기 중인 Promise를 `calls` 배열에 쌓는다. 배열 순서가 곧 서버 쓰기 순서다.
  `settle(calls[i], 'resolve' | 'reject')`로 어느 요청을 언제 끝낼지 테스트가 정한다.
- 테스트 13개: `applyMove` 4개 (end / keep / 불변성 / 없는 id), 롤백 4개 (카드별 확정 상태 / A 실패가 B를 지우지 않음 /
  첫 성공 + 둘째 실패 / keep 롤백 제자리), 경쟁 상태 5개 (직렬화와 쓰기 순서 / 실패 후에도 줄 유지 /
  다른 카드끼리 대기 없음 / 지난 성공 응답 무시 / 지난 실패 응답은 롤백·알림 없음).
- 이슈 #11 수용 기준 6개가 각각 최소 1개 테스트에 대응한다.

### 리뷰 / 검증
- **1차 실행에서 13개 중 8개가 타임아웃(5초)이었다. 원인은 테스트 헬퍼였다.** `async function move`가
  이동 결과 Promise를 그대로 `return`해서, 호출부의 `await move(...)`가 그 결과가 끝날 때까지 기다렸다.
  결과는 테스트가 `settle`해야 끝나므로 서로를 기다리며 멈췄다. 통과한 4개가 모두 `move`를 쓰지 않는
  `applyMove` 테스트라는 점에서 헬퍼를 의심했다. `{ outcome }`으로 감싸 돌려주도록 고쳤다.
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트). `git diff`와 새 파일을 다시 읽고 했다.
  - 버그·엣지케이스: 미통과. `keep` 이동 후 롤백하는 경로를 검증하지 않았다.
  - 요구 누락: 통과. 단 ⑤의 서버 쓰기 순서는 선택한 설계대로 moveStage 호출 순서로 확인하며, localStorage 값은 보지 않는다.
  - 구조: 미통과. `@testing-library/dom`(RTL 필수 peer)이 package.json에 없고 npm 자동 설치에 기대고 있었다.
  - 접근성: 해당 없음.
  - 미사용 코드: 미통과. `applyMove` export는 파일 밖 사용처 1곳(테스트)을 grep으로 확인했다. `PendingCall.placement`는 기록만 하고 단언이 없었다.
  - 세 건 모두 채택해 반영했다 (DECISIONS.md "채택: rollback-test 리뷰 3건").
- **변이 확인**: 테스트가 통과하는 것만으로는 회귀를 잡는다고 말할 수 없다. 그래서 소스에 버그 7개를
  하나씩 심고 실행한 뒤 원본으로 되돌렸다.

  | 심은 버그 | 깨진 테스트 |
  |---|---|
  | M1 카드별 줄 제거 (즉시 전송) | 직렬화와 쓰기 순서 |
  | M2 성공 응답의 시퀀스 검사 제거 | 지난 성공 응답 무시 |
  | M3 실패 응답의 시퀀스 검사 제거 | 지난 실패 응답은 롤백·알림 없음 |
  | M4 superseded 성공 시 확정 상태 미갱신 | 첫 성공 + 둘째 실패 |
  | M5 모든 카드를 확정 상태로 롤백 (스냅샷식) | A 실패가 B를 지우지 않음 |
  | M6 롤백 자리 규칙을 end로 고정 | keep 롤백 제자리 |
  | M7 `applyMove`가 keep 무시 | keep 단위 테스트 + keep 롤백 |

  7개 모두 의도한 테스트가 깨졌고, 복원 후 13개 통과와 소스 diff가 `export` 한 줄뿐인 것을 확인했다.
- 10회 반복 실행해 10회 모두 13/13 통과했다. 난수와 타이머를 쓰지 않아 결정적이다.
- `npm test` / `npm run typecheck` / `npm run lint` / `npm run build` 통과.
- 사용자 요청으로 이슈 #11을 `gh issue close`로 직접 닫았다. `Closes #11`은 main에 병합될 때 동작하는데, 병렬 브랜치라 아직 병합되지 않았기 때문이다.
- 브라우저 검증은 하지 않았다. 화면 동작을 바꾸지 않았기 때문이다 (소스 변경은 `export` 한 줄). 그래서 포트 5176은 쓰지 않았다.
## [chosung-search] 초성만으로 이름 검색

모델: Claude Opus 5.5 (1M context)

### 프롬프트 1
병렬 워크트리 작업 지시 중 이 기능에 해당하는 부분 (앞부분은 워크트리 규칙: node 경로, 포트, 기록, 커밋 형식, 다른 워크트리 금지).
> 담당: 이슈 #18 [chosung-search] 초성만으로 이름 검색. 포트 5174.
> 건드릴 파일: src/features/applicants/useApplicantFilter.ts 와 새 한글 유틸. 다른 파일은 손대지 마라.
> gh issue view 18 으로 요구사항과 수용 기준을 먼저 읽어라.

### 프롬프트 2 — 가정·설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 혼합 입력(`한ㅅ`): **글자별 혼합 매칭** (대안: 이슈 규칙대로 섞이면 기존 부분 일치 → `한ㅅ`은 0건)
- 초성 계산 시점: **필터할 때마다 계산** (대안: applicants 변경 시 id→초성 Map 미리 계산)
- 유틸 위치: **`src/shared/hangul.ts`** (대안: `src/features/applicants/hangul.ts`)

### 프롬프트 3 — 리뷰 제안 채택 (막힌 지점)
처음 응답에서 서로 모순되는 1a(export 제거)와 1b(export 유지)가 함께 선택되었다. 다시 묻자 사용자가 질문을 멈추고 이렇게 요청했다.
> 이걸 진행했을때 장단점 분석해줘

분석을 본 뒤 **1a(export 제거)**를 골랐고, 처음에 고르지 않았던 **2번(초성 판정을 루프 밖으로)도 채택**으로 바꿨다. 3번(순회 단위 통일)은 처음부터 채택.

### 프롬프트 4 — 브라우저 검증
Claude in Chrome 확장이 응답하지 않아 "재시도 / 직접 확인" 중에서 물었다.
> 1번

> http://localhost:5173/ 연결되었어

5173은 다른 브랜치의 개발 서버라서, 이 브랜치 서버인 5174에서 검증했다.

### AI 출력 요지
- 음절의 초성은 `(코드 - 0xAC00) / 588`의 몫이다. 초성 표는 IME가 입력하는 호환 자모(U+3131~) 19자이며, 겹받침 자음(`ㄳ` 등)은 표에 없어서 초성으로 치지 않는다.
- 비교는 글자 단위다. 검색어 글자가 초성이면 대상 음절의 초성과, 아니면 글자 그 자체와 비교한다. 대상 위를 한 칸씩 밀며 부분 일치를 찾는다. 그래서 `ㅎㅅㅂ`, `ㅅㅂ`, `한ㅅ`, `ㅎ수`가 모두 "한수빈"과 맞는다.
- 검색어에 초성이 없으면 기존 `includes`를 그대로 쓴다. 완성 음절 검색 결과는 이 기능 전과 동일하다.
- 공개 API는 `createChosungMatcher(keyword)` 하나다. 검색어를 받아 대상마다 호출할 함수를 돌려주고, 초성이 있는지는 검색어당 한 번만 판정한다. 훅은 `useMemo` 안에서 매처를 한 번 만들어 1,000건에 재사용한다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트). `git diff`를 처음부터 다시 읽었다.
- 체크리스트: 버그·엣지 통과 / 요구 누락 통과 / 구조 통과 / 접근성 미통과(제안) / 미사용 export **미통과**.
- 무엇이 문제였나:
  (1) `isChosung`·`getChosung`이 export되어 있었지만, grep으로 세어 보니 파일 밖 사용처가 0이었다. 이 프로젝트에서 반복된 실수 유형이다.
  (2) `[...keyword].some(isChosung)`이 지원자마다, 즉 필터 한 번에 1,000번 실행되었다. 검색어 판정은 한 번이면 된다.
  (3) 초성 판정은 코드 포인트 단위(`[...keyword]`), 비교 루프는 코드 유닛 단위(`keyword[i]`)로 순회 단위가 서로 달랐다.
  (4) placeholder가 "지원자 이름"뿐이라 초성 검색이 된다는 걸 알 수 없다.
  (5) IME로 "한"을 치는 도중인 "하" 상태에서는 여전히 0건이다(이 기능 전부터 있던 동작).
- 채택 / 수정 / 기각:
  - (1) 채택. export를 떼고 파일 내부 함수로 두었다. "유지"도 한때 선택지에 있었다. 장단점을 비교한 결과, 테스트 러너가 아직 없고 `ㅎㅅㅂ`·`ㄳ` 케이스로 공개 매처를 테스트하면 계산식도 간접적으로 검증된다는 점을 보고 제거로 정했다.
  - (2)·(3) 채택. 인자를 늘리는 대신 검색어로 매처 함수를 만들어 돌려주는 형태로 바꿨고, 판정 루프를 코드 유닛 단위로 통일했다.
  - (4)·(5) 이월. (4)는 지정된 수정 범위 밖인 `FilterBar.tsx`를 바꿔야 하고, (5)는 마지막 음절 분해가 필요해 범위가 커진다. 선택지에서 뺐고 사용자가 추가하지 않았다.
- 직접 확인한 것:

  | 확인 | 방법 | 결과 |
  |---|---|---|
  | 매처 케이스 16개 (`ㅎㅅㅂ`·`ㅅㅂ`·`한수`·`한ㅅ`·`ㅎ수`·`ㄳ`·`ㄲ`≠`ㄱ`·`힣`·영문 등) | scratchpad node 스크립트 | 16/16 통과 |
  | 시드 1,000건 | node, `createSeedApplicants(1000)` | `ㄱㅅㅇ` 23명(강서연 포함), `ㅅㅇ` 169, `강서` 5, `강ㅅ` 15 |
  | 1,000건 필터 비용 | node, 100회 평균 | 검색어 4종 모두 0.1ms 미만 |
  | `ㅎㅅㅂ` | 브라우저 5174, 입력란에 타이핑 | 16명(한수빈·황수빈·홍수빈), 안내 문구 "16명이 검색되었습니다." |
  | 혼합 `한ㅅ` | 브라우저 | 15명 (한수빈·한시우·한서연·한수아…) |
  | 직무 AND | 브라우저 | `ㄱ` 181 → `ㄱ` + 프로덕트 디자이너 24, 직무만 116 |
  | 겹받침 `ㄳ` | 브라우저 | 0명 |

- **확인하지 못한 것**: 브라우저 입력은 확장의 `type`과 JS로 글자를 완성된 상태로 넣었다. 실제 한글 IME의 조합(composition) 과정은 재현하지 않았다. 따라서 "타이핑 도중 `강ㅅ` 상태에서 깜빡이지 않는다"는 매처 결과로만 확인했고, 실제 조합 입력으로는 확인하지 않았다.
- 실패 경로(15%)·persist: 이 기능은 API를 호출하지 않는 순수 필터라 해당 없음.
- `npm run typecheck` / `npm run lint` / `npm run build` 통과.

## [detail-panel] 지원자 상세 drawer

모델: Claude Opus 5.5 (1M context)

### 프롬프트 1 — 병렬 워크트리 작업 지시
> 이 워크트리는 recruitment-pipeline-board 과제의 병렬 브랜치다. main은 커밋 51b88b4까지 동기화되어 있다.
>
> 규칙:
> 1) /feature-step 스킬 사이클을 그대로 따른다. 특히 요구사항 해석·설계 선택·리뷰 제안 채택은 반드시 AskUserQuestion으로 사용자에게 묻는다. 사용자가 이 페인을 보고 있으니 직접 답한다. 네가 대신 결정하고 판단했다고 기록하지 마라.
> 2) node가 기본 16이라 실패한다. 명령 앞에 export PATH=/opt/homebrew/opt/node/bin:$PATH 를 붙여라.
> 3) 개발 서버와 브라우저 검증에는 아래 지정된 포트를 써라. 다른 브랜치가 5173~5176을 쓰고 있다.
> 4) PROMPTS.md / DECISIONS.md / README.md 도 평소대로 기록한다. main 병합 시 충돌은 양쪽을 살리는 방향으로 해결할 것이다.
> 5) 커밋 본문 끝에 Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com> 를 넣고 Closes #N 으로 이슈를 닫는다. push는 자기 브랜치로만 한다.
> 6) 다른 워크트리 디렉터리는 절대 건드리지 마라.
>
> 담당: 이슈 #8 [detail-panel] 지원자 상세 보기. 포트 5177.
> 건드릴 파일: 새 drawer 컴포넌트, ApplicantCard.tsx, Board.tsx. useApplicants.ts 와 MoveToasts 는 다른 브랜치가 쓰고 있으니 피하라.
> gh issue view 8 의 코멘트에 4컬럼 구조와 키보드 가정(위 화살표로 열기, ESC/아래로 닫기)이 정리되어 있다.

### 프롬프트 2 — 요구사항 가정 (AI가 제시한 선택지에서 사용자가 선택)
- 클릭 대상: **이름을 버튼으로 + 카드 전체 클릭** (대안: 이름 버튼만 / 카드 전체를 버튼으로)
- `↑` 열기 / `↓` 닫기: **이번에 넣는다** (대안: #10으로 미룬다)
- drawer 안 단계 이동: **읽기 전용** (대안: 이전/다음 버튼 제공)
- URL 반영: **반영 안 함** (대안: `?applicant=id`)

### 프롬프트 3 — 설계 선택
- drawer 방식: **네이티브 `<dialog>` 모달 drawer** (대안: 비모달 `<aside>` 사이드 패널)
- 데이터 원천: **선택한 id만 들고 목록에서 찾기** (대안: 열 때의 객체 스냅샷)

### 프롬프트 4 — 리뷰 제안 채택
1번(텍스트 선택 시 열지 않기) 채택. 2번 기각 — "방어 코드로 남긴다". 3번 기각 — "키 규칙을 한 가지로 유지".

### 프롬프트 5 — 막힌 지점: Chrome 확장 미연결
> 확장을 연결할 테니 네가 확인해라

(재시도했으나 확장이 여전히 연결되지 않아 멈추고 보고함)

### 프롬프트 6 — 막힌 지점 해결: 헤드리스 Chrome + CDP로 검증
> 브라우저 검증은 Claude in Chrome 확장 대신 헤드리스 Chrome + CDP로 해라. 확장은 한 세션만 붙을 수 있어 병렬 세션에서는 막힌다. 이 방법은 확장이 필요 없고 세션끼리 충돌하지 않는다.
>
> (개발 서버 실행 → `--headless=new --remote-debugging-port` 로 Chrome 실행 → Node 내장 WebSocket으로 CDP 호출 → 조회 실패 대비 `Page.reload` 루프 → 클릭은 `element.click()` 절차)
>
> 주의: 측정 스크립트를 짤 때 "항상 참이 되는 식"을 쓰지 않도록 조심해라. 나는 이번 과제에서 그런 실수를 두 번 했다. 통과했다고 기록했는데 아무것도 증명하지 못한 검사였다. 비교 대상을 실제로 읽어서 맞대어라.
>
> 내 포트 5177, CDP 포트는 9227, 프로필은 /tmp/chrome-detail 을 써라. 확장 연결을 기다리지 말고 이 방법으로 검증을 끝내라.

(가운데 절차 4단계는 명령·코드 조각이라 요지로 줄였다.)

### AI 출력 요지
- 새 `ApplicantDrawer`는 결과 확인 창과 같은 `showModal()` 패턴이다. ESC(`cancel` 이벤트)·`↓`·✕·배경 클릭이 모두 `onClose` 하나로 모인다.
- 카드: 이름을 `<button data-detail>`로 바꿨다. `li` 클릭은 `closest('button, a')`로 안쪽 버튼을 걸러내는 마우스 전용 보조 경로이고, 키보드는 이름 버튼과 `↑`가 맡는다.
- 보드: `detailId`만 상태로 들고 `applicants`에서 찾는다. 닫을 때 `focusAfterRender`로 그 카드의 이름 버튼을 직접 잡는다. 카드 빈 곳을 마우스로 눌러 연 경우 브라우저가 돌려줄 포커스가 없기 때문이다.
- `formatAppliedAt`을 카드와 drawer가 함께 쓰게 되어 `src/shared/date.ts`로 옮겼다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트, `git diff` 재독). 새 export 2개는 사용처를 셌다(`formatAppliedAt` 2곳, `ApplicantDrawer` 1곳).
- 제안 3개: 1번 채택, 2·3번 기각 (DECISIONS.md "AI 제안 채택/기각").
- **테스트에서 버그 하나를 찾았다.** 390px 모바일에서 drawer가 `x=10, 너비 380`으로 왼쪽에 10px 틈이 남았다. `width: min(380px, 100vw)`는 380px보다 좁은 화면에서만 꽉 차기 때문이다. 설계 때 "좁은 화면에서는 전체 화면"이라고 말했지만 실제로 그렇게 만들지 않았던 것이다. 768px 이하 미디어 쿼리에 `width: 100vw`를 넣어 고쳤고, 다시 재서 `x=0, 너비 390`을 확인했다.
- 직접 확인한 엣지케이스 (헤드리스 Chrome + CDP, 포트 5177/9227). 모든 검사는 읽은 값을 기대값과 맞대었고, 참이 되기 쉬운 검사에는 **반대 결과가 나와야 하는 대조 검사**를 짝으로 두었다.

  | 검사 | 비교한 값 | 결과 |
  |---|---|---|
  | 카드 빈 곳(직무) 클릭 → 열림 | drawer 제목 = 카드 이름 | 통과 |
  | 이름 버튼 클릭 → 열림, 첫 포커스 | 제목 = 카드 이름, 포커스 = "상세 닫기" | 통과 |
  | 비활성 `←` 버튼을 실제 마우스 좌표로 클릭 | `open=false` (disabled=true 확인) | 통과 |
  | 결과 칩 클릭 | `open=false` + 대조: 칩 텍스트는 실제로 바뀜 | 통과 |
  | ESC / `↓` / 배경 실제 마우스 클릭 / ✕로 닫기 (4경로, 모두 마우스로 연 뒤) | 열림 확인 → 닫힘 → `activeElement`의 `data-detail` = 카드 id | 4개 통과 |
  | 대조: drawer 안(제목)을 마우스로 클릭 | 열린 채 유지 | 통과 |
  | 카드의 `→` 버튼 포커스에서 `↑` (실제 키 이벤트) | 제목 = 카드 이름 | 통과 |
  | 대조: 검색창 포커스에서 `↑` | 열리지 않음 | 통과 |
  | 이름 텍스트를 선택한 채 카드 클릭 / 선택 해제 후 같은 클릭 | 안 열림 / 열림 | 통과 |
  | **롤백 중 열린 drawer**: `Math.random`을 0.01로 고정해 실패를 확정, `→` 직후 상세 열기 | 직후 단계 "면접" → 1.2초 뒤 "서류검토", 열린 채 유지, 카드도 실제로 서류검토 컬럼 | 통과 |
  | 롤백 동안 포커스 | `activeElement`가 drawer 안에 머묾 (보드의 포커스 추적이 모달 밖으로 끌어내지 않음) | 통과 |
  | 롤백 후 닫기 | 포커스 = 옮겼던 카드 이름 버튼 | 통과 |
  | 데스크톱 1400×900 | 너비 380, 오른쪽 끝 = 화면 폭, 높이 = 화면 | 통과 |
  | 모바일 390×800 | 1차 `[10, 380, 800]` **실패** → 수정 후 `[0, 390, 800]` | 수정 후 통과 |
  | 모바일 닫기 버튼 / ESC 후 포커스 | 44×44, 포커스 복귀 | 통과 |
  | 새로고침 (URL 미반영 가정) | 열린 상태에서 reload → `open=false` | 통과 |

  최종 26/26 + 새로고침 1건 통과. 실패 주입은 페이지 안의 `Math.random`만 바꿨고 mock API 코드(지연 200~800ms, 15% 실패)는 건드리지 않았다.
- `npm run typecheck` / `npm run lint` / `npm run build` 통과. 단위 테스트 스크립트는 아직 없다(#11 rollback-test 범위).

## [loading-error-empty] 로딩 · 에러 · 빈 상태

모델: Claude Opus 5.5 (1M context)

### 프롬프트 1
병렬 워크트리 작업 지시 중 이 기능에 해당하는 부분 (앞부분은 워크트리 규칙: node 경로, 포트, 기록, 커밋 형식, 다른 워크트리 금지).
> 담당: 이슈 #9 [loading-error-empty] 로딩 · 에러 · 빈 상태. Must 요구사항이다. 포트 5178.
> useApplicants.ts 는 feat/undo 가 되돌리기 기능으로 고치는 중이니 꼭 필요한 부분(로드 실패 재시도)만 최소로 건드려라. Board.tsx 와 ApplicantCard.tsx 는 feat/detail-panel 이 쓰고 있으니 피해라. Column.tsx 와 App.tsx 는 네 영역이다.
> 브라우저 검증은 Claude in Chrome 확장 대신 헤드리스 Chrome + CDP로 해라. (…) 조회가 15% 확률로 실패하므로 보드가 뜰 때까지 Page.reload 를 반복하는 루프를 먼저 둬라. 에러 상태는 이 실패율 덕에 저절로 재현된다. 재시도가 실제로 복구시키는지까지 확인해라.
> 측정 스크립트에 "항상 참이 되는 식"을 쓰지 마라. 나는 이번 과제에서 그런 실수를 두 번 했고, 통과했다고 적었지만 아무것도 증명하지 못한 검사였다. 비교 대상을 실제로 읽어서 맞대어라.
> gh issue view 9 로 요구사항과 수용 기준을 먼저 읽어라. 검색 결과 0건 처리는 #7에서 보드 전체 0건만 안내하도록 했고, 컬럼별 빈 상태는 이 이슈로 미뤄져 있다는 코멘트도 확인해라.

### 프롬프트 2 — 가정 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 재시도: **수동 재시도 버튼만** (대안: 자동 재시도 N회 후 버튼)
- 로딩 UI: 선택지 "컬럼 스켈레톤"에 대해 사용자가 직접 입력 — "컬럼 고스트 스켈레톤 형태로 가자" (대안: 스피너 + 문구)
- 보드 전체 0건: **지금처럼 안내로 대체 + 조건 초기화 버튼** (대안: 버튼 없이 유지 / 보드 유지 + 컬럼마다 빈 상태)
- 빈 컬럼 문구: **단일 문구** (대안: 필터 여부로 문구 구분 — Board.tsx 수정 필요)

### 프롬프트 3 — 설계 선택
- 재시도 구현: **시도 번호를 조회 effect 의존성에 넣기** (대안: load 함수를 useCallback으로 추출 — diff가 커져 feat/undo와 충돌 위험)
- 스켈레톤: **실제 Column 재사용 + 스켈레톤 카드** (대안: 독립 마크업)

### 프롬프트 4 — 리뷰 제안 채택
- 1(aria-busy 제거)·2(포커스 이동)·3(스켈레톤에 탭 자리)·4(reload 단위 테스트) 모두 채택. 5(Board.module.css 결합)는 **그대로 둠**.

### 프롬프트 5 — 검증 중 발견한 문제 (막힌 지점)
CDP 측정에서 스켈레톤 → 보드 전환 시 컬럼이 79px(모바일 91px) 밀리는 것이 나와 선택지를 제시했다.
- **로딩 중에도 필터바를 비활성으로 표시** (대안: 같은 높이의 빈 자리 / 그대로 둠)

### 프롬프트 6 — 커밋 모델 표기
지시문의 Co-Authored-By는 "Claude Opus 5"였지만, 실제 모델(claude-opus-5-5)과 d0f636b의 통일 결정이 달라 선택지를 제시했다.
- **Claude Opus 5.5 (1M context)** (대안: 지시문 그대로 Opus 5)

### AI 출력 요지
- `useApplicants`: `loadAttempt` 상태를 두고 조회 effect의 의존성을 `[]` → `[loadAttempt]`로 바꿨다. `reload()`는 status를 `loading`으로 돌리고 번호를 올린다. 앞선 시도의 늦은 응답은 기존 `cancelled` 가드가 버린다. 변경 10줄.
- `BoardSkeleton`: 실제 `Column`에 `count={null}`(인원 수 자리 비움)과 회색 카드 4장을 넣어 그린다. 모바일 탭 막대 자리도 `ColumnTabs.module.css` 클래스로 그린다. 회색 자리는 `aria-hidden`이고, 스크린리더에는 `role="status"` 문장 하나만 전달한다.
- `Column`: `count === 0`이면 빈 `<ul>` 대신 "이 단계에 지원자가 없습니다." 안내를 둔다. Board.tsx는 건드리지 않았다.
- `App`: 에러 시 `role="alert"` + "다시 시도" 버튼. 재시도가 또 실패하면 새로 뜬 버튼으로 포커스를 돌린다. 보드 전체 0건이면 "검색 조건 초기화" 버튼을 두고, 누르면 검색란으로 포커스를 옮긴다. 필터바는 로딩 중에도 `disabled`로 떠 있다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트). `git diff`를 처음부터 다시 읽었다.
- 체크리스트: 버그·엣지 **미통과** / 요구 누락 통과 / 구조 제안 1 / 접근성 **미통과** / 미사용 export 통과 (`reload`·`BoardSkeleton`·`count: null` 모두 파일 밖 사용처를 grep으로 확인).
- 무엇이 문제였나 (리뷰):
  (1) 스켈레톤의 `role="status"`가 `aria-busy="true"` 컨테이너 안에 있었다. 보조 기술이 busy 영역의 알림을 미루거나 생략할 수 있다.
  (2) "다시 시도"·"검색 조건 초기화"는 누르는 순간 자신이 사라져 포커스가 body로 떨어진다.
  (3) 스켈레톤에 모바일 탭 막대가 없어, 보드가 뜨면 막대가 끼어들며 화면이 밀린다.
  (4) 재시도 단위 테스트가 없다.
  (5) BoardSkeleton이 `Board.module.css`를 가져다 쓰는 결합 — 그대로 둠 (레이아웃 정의가 한 곳).
- 채택 / 수정: (1)~(4) 채택. (4)는 feat/undo가 `useApplicants.test.ts`를 고칠 수 있어 `useApplicants.load.test.ts`로 파일을 나눴다.
- **테스트가 헛돌지 않는지 확인**: 의존성을 일부러 `[]`로 되돌리고 돌리자 새 테스트 2개가 모두 실패했다(`expected length 2 but got 1` — 재시도가 조회를 다시 부르지 않음). 되돌린 뒤 15/15 통과.
- **검증에서 새로 발견한 문제**: 스켈레톤과 실제 보드의 컬럼 위치를 컬럼마다 맞대자 x·폭·헤더 높이는 0 차이였지만 **y가 79px(모바일 91px)** 달랐다. 필터바가 `ready` 때만 나타나기 때문이다. 스켈레톤을 고른 이유(전환 시 레이아웃이 튀지 않음)가 지켜지지 않고 있었다. 사용자 선택으로 필터바를 로딩 중에도 비활성으로 띄웠다.
- **측정 스크립트의 실수와 수정**: 필터바를 로딩 중에도 띄우자, "`#applicant-search`가 있으면 ready"라는 판정이 로딩 중에도 참이 되었다. 두 번째 측정은 스켈레톤을 스켈레톤과 비교해 차이 0이 나왔다. "복구 후 카드 16장(=스켈레톤 4×4), 헤더 인원 수 빈칸"이라는 값을 보고 알아챘다. 판정을 "입력란 활성 + 로딩 안내 없음"으로 고쳐 다시 쟀다. 아래 표는 고친 뒤의 값이다.
- 직접 확인한 것 (헤드리스 Chrome 1400×900 + CDP, 포트 5178, 시드 1,000건):

  | 확인 | 방법 | 결과 |
  |---|---|---|
  | 로딩 스켈레톤 | 새로고침 직후 20ms 간격 스냅샷 | 컬럼 4개(서류검토·면접·처우협의·결과), 카드 자리 4/4/4/4, 인원 수 빈칸, 검색란 disabled, `aria-busy` 0개 |
  | 전환 시 레이아웃 | 스켈레톤 첫 모습과 ready 모습의 컬럼 rect 비교 (ready는 카드 1,000장, 인원 434/236/118/212명) | 4개 컬럼 모두 dx·dy·폭·헤더 높이·목록 시작점 차이 0 (수정 전 dy 79) |
  | 모바일 400px 전환 | 같은 방법, 보이는 컬럼 | y 218.5 → 218.5, 폭 376 → 376 (수정 전 127.5 → 218.5) |
  | 에러 UI | 에러가 날 때까지 새로고침 반복 (3회째) | `role="alert"` "지원자 목록을 불러오지 못했습니다." + "다시 시도" |
  | 재시도 복구 | 버튼 클릭 → 추적 | 1회차 재실패(스켈레톤 거쳐 다시 에러, 포커스 "다시 시도"), 2회차 복구. 복구 후 카드 1,000장, 헤더 434/236/118/212명 = localStorage에서 센 값과 일치 |
  | 재시도 통계 | 에러 상태에서 재시도를 60회 누르고, 성공하면 에러가 날 때까지 새로고침 | 재시도 50 복구 / 10 재실패(16.7%), 새로고침 386 / 50(11.5%). 막힘 없이 독립 시행으로 동작 |
  | 빈 컬럼 | localStorage에서 "일부 컬럼만 0명"이 되는 이름을 골라 검색 ("오다은") | 기대 1/1/1/0 = 표시 1명/1명/1명/0명, 결과 컬럼에 li 0개 + "이 단계에 지원자가 없습니다." |
  | 검색 0건 + 초기화 | "존재하지않는이름zz" 입력 → 버튼 클릭 | 안내 문구 표시, 클릭 후 검색어 "" · 직무 전체 · 카드 1,000장 · 포커스 `applicant-search` |

- 첫 측정에서는 재시도가 연속 4번 실패했다(15%면 약 0.05% 확률). 결함을 의심해 위 60회 통계를 따로 쟀고, 실패율이 설정과 맞아 우연으로 판단했다. 원인을 따로 찾지는 않았다.
- **확인하지 못한 것**: 스크린리더 실기기 낭독(`role="status"`·`alert`가 실제로 읽히는지)은 확인하지 않았고 DOM 속성만 확인했다. 재시도로 복구된 뒤 포커스는 body에 남는다(측정값). 에러 화면에서 필터바가 숨는 것은 코드(`status !== 'error'`)로만 확인했다.
- persist: 이 기능은 쓰기를 하지 않는다. 복구 후 인원 수가 localStorage 값과 같은 것만 확인했다.
- `tsc -b` / `npm run lint` / `npm test` 15/15 / `npm run build` 통과.
- main(detail-panel #8 병합분) 병합 후 같은 스크립트로 다시 측정: 레이아웃 차이 0(데스크톱 4컬럼·모바일), 빈 컬럼 1/1/1/0 일치, 재시도 1회차 복구 후 434/236/118/212명 일치, 초기화 후 포커스 검색란. 충돌은 PROMPTS.md(두 섹션 모두 유지, 병합 순서대로)와 README.md 구현 범위 표(두 행 모두 완료로)였다.

---

## [undo] 되돌리기

모델: Claude Opus 5.5 (1M context)

### 프롬프트 1
> 이 워크트리는 recruitment-pipeline-board 과제의 병렬 브랜치다. main은 커밋 51b88b4까지 동기화되어 있다.
>
> 규칙:
> 1) /feature-step 스킬 사이클을 그대로 따른다. 특히 요구사항 해석·설계 선택·리뷰 제안 채택은 반드시 AskUserQuestion으로 사용자에게 묻는다. 사용자가 이 페인을 보고 있으니 직접 답한다. 네가 대신 결정하고 판단했다고 기록하지 마라.
> 2) node가 기본 16이라 실패한다. 명령 앞에 export PATH=/opt/homebrew/opt/node/bin:$PATH 를 붙여라.
> 3) 개발 서버와 브라우저 검증에는 아래 지정된 포트를 써라. 다른 브랜치가 5173~5176을 쓰고 있다.
> 4) PROMPTS.md / DECISIONS.md / README.md 도 평소대로 기록한다. main 병합 시 충돌은 양쪽을 살리는 방향으로 해결할 것이다.
> 5) 커밋 본문 끝에 Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com> 를 넣고 Closes #N 으로 이슈를 닫는다. push는 자기 브랜치로만 한다.
> 6) 다른 워크트리 디렉터리는 절대 건드리지 마라.
>
> 담당: 이슈 #12 [undo] 되돌리기. 포트 5175.
> 건드릴 파일: src/features/applicants/useApplicants.ts 와 MoveToasts. Board.tsx 는 가능하면 피하라.
> gh issue view 12 로 요구사항을 읽고, moveStage 의 placement 인자(end/keep)가 되돌리기 위치 선택지가 된다는 코멘트도 확인하라.

### 프롬프트 2 — 가정 (AI가 제시한 선택지에서 사용자가 선택)
- 히스토리 깊이: **이동마다 토스트 1개** (대안: 깊이 1 / 깊이 N 스택)
- 시간 제한: **토스트 표시 시간(5초)까지** (대안: 되돌리기 토스트만 8초 / 제한 없음)
- 되돌리기 전에 같은 카드를 또 옮기면: **이전 되돌리기 토스트 무효화** (대안: 그대로 두고 그 시점 단계로 복귀)
- 키보드: **토스트 버튼 Tab 접근 + Ctrl/⌘+Z** (대안: 토스트 버튼만)

### 프롬프트 3 — 설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 되돌린 카드의 자리: **원래 위치 정확 복원 (앞 카드 id 기준)** — AI 추천(원래 이동과 같은 placement)이 아닌 선택
  (대안: 원래 이동과 같은 placement / 항상 `'end'`)
- 토스트 표시 시점: **서버가 이동을 확정한 뒤** (대안: 클릭 즉시)
- 되돌린 뒤 포커스·모바일 탭: **범위 밖, Board는 건드리지 않음** (대안: Board까지 손대 카드로 포커스·탭 이동)
- anchor가 사라졌을 때: **전역 배열에서 anchor 바로 뒤, 못 찾으면 끝** (대안: 같은 컬럼이 아니면 무조건 컬럼 끝 / 앞·뒤 두 anchor)
- 되돌리기 성공 피드백: **토스트 없음** (대안: "되돌렸습니다" 안내 토스트)

### 프롬프트 4 — 리뷰 제안 채택
리뷰 6건(1 KeyZ 코드, 2 repeat 무시, 3 hover/focus 중 타이머 정지, 4 일반 이동 롤백도 정확 복원, 5 인자 옵션 객체, 6 `insertAfter`를 `shared/`로) 모두 채택.

### 프롬프트 5 — 브라우저 검증 방법 (막힌 지점)
Chrome 확장이 연결되지 않아 "확장 연결 후 Claude가 확인"을 선택했고, 이어서:
> http://localhost:5173/ 연결되었어

확장으로 5175를 열었지만 창이 가려진 탭(`visibilityState: hidden`)이라 타이머가 몰려 실행되어 실패 경로 검증이 계속 시간 초과됐다. 이어진 지시:
> 브라우저 검증은 Claude in Chrome 확장 대신 헤드리스 Chrome + CDP로 해라. 확장은 한 세션만 붙을 수 있어 병렬 세션에서는 막힌다. 이 방법은 확장이 필요 없고 세션끼리 충돌하지 않는다.
> (…개발 서버 · `--headless=new --remote-debugging-port` · Node 내장 WebSocket으로 `Runtime.evaluate` · 조회 실패 대비 `Page.reload` 루프 · `element.click()`으로 실제 DOM 이벤트…)
> 주의: 측정 스크립트를 짤 때 "항상 참이 되는 식"을 쓰지 않도록 조심해라. 나는 이번 과제에서 그런 실수를 두 번 했다. 통과했다고 기록했는데 아무것도 증명하지 못한 검사였다. 비교 대상을 실제로 읽어서 맞대어라.
> 내 포트 5175, CDP 포트는 9225, 프로필은 /tmp/chrome-undo 를 써라.

### 프롬프트 6 — 검증에서 나온 문구 문제
되돌리기 실패 토스트가 "이동하지 못해 면접 단계로 되돌렸습니다"로 읽히는 문제에 **되돌리기 실패 전용 문구** 선택 (대안: 그대로 둠).

### AI 출력 요지
- **되돌리기는 별도 경로가 아니라 같은 이동이다.** 원래 단계·원래 자리로 한 번 더 `startMove`를 탄다.
  카드별 직렬화, 늦은 응답 무시, 실패 롤백을 새로 만들지 않고 그대로 물려받는다.
- **정확 복원**: 이동 직전 "같은 컬럼에서 바로 앞 카드 id"(anchor)를 기억한다. mock `moveStage`에
  `{ after: id | null }` placement를 추가했다. 컬럼은 전역 배열을 걸러낸 부분열이라 앞 카드 뒤에 끼우면 컬럼 안
  순서가 돌아온다. 서버와 화면이 같은 `insertAfter`(`src/shared/order.ts`)를 쓰므로 규칙이 갈리지 않는다.
- 토스트는 서버 확정 뒤에만 되돌리기 버튼을 달고 뜬다. 같은 카드를 다시 옮기면 그 카드의 옛 토스트를 즉시 지운다.
- 롤백 기준: 모든 이동이 "이동 직전 자리"(`currentSpot`)로 롤백한다. 서버는 실패한 요청에서 카드를 움직이지
  않았으므로 새로고침해도 같은 자리다 (리뷰 4번으로 일반 이동까지 확장).
- Ctrl/⌘+Z는 가장 최근 되돌리기 토스트를 실행한다. 입력창·모달(`dialog[open]`)·키 반복에서는 무시한다.
- 토스트에 마우스를 올리거나 포커스를 두면 모든 토스트 시간이 멈추고, 벗어나면 남은 시간부터 이어간다.
- 변경 파일: `useApplicants.ts`, `MoveToasts.tsx/.module.css`, `api/mock.ts`, `shared/order.ts`(신규), `App.tsx`(props 연결 2줄). `Board.tsx`는 건드리지 않았다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트, `git diff`를 처음부터 다시 읽음). 새 export(`insertAfter`, `Toast.undo`)는 파일 밖 사용처를 셌고 `UndoTarget`은 export하지 않았다.
- 무엇이 문제였나 (리뷰):
  1. 한글 입력 상태에서 `event.key`가 `'ㅋ'`라 Ctrl+Z가 안 먹을 수 있다 → `event.code === 'KeyZ'`도 본다.
  2. 키를 꾹 누르면 쌓인 되돌리기가 연달아 실행된다 → `event.repeat` 무시.
  3. 버튼까지 Tab으로 가는 동안 5초가 지나 토스트가 사라진다 (WCAG 2.2.1) → hover/focus 중 타이머 정지.
     포커스를 가진 버튼이 토스트와 함께 사라지면 blur 없이 body로 가므로, 토스트가 바뀔 때마다 포커스 위치를 다시 본다.
  4. `'end'` 이동이 실패하면 화면은 맨 아래로, 서버는 원래 자리 → 새로고침 시 카드가 튄다 (기존 문제) → 이동 직전 자리로 롤백.
  5·6. 인자 6개 → `MoveContext` 객체, `insertAfter`를 mock 모듈에서 `shared/order.ts`로.
- 구현 중 lint(`react(immutability)`)가 ref 안 타이머 객체를 제자리 수정한 줄을 잡아, 새 객체로 교체하는 방식으로 고쳤다.
- **검증 스크립트에서 내 실수 세 가지를 잡았다.** 모두 "통과"가 나왔거나 나올 뻔한 경우다.
  1. 첫 실행의 A·C가 PASS였지만 대조군 `notLast: false` — 대상 카드가 컬럼 **맨 끝**(432/433)에 있었다.
     그 자리에서는 `'end'` 규칙으로도 통과하므로 정확 복원을 전혀 증명하지 못한다. 대상을 매번
     "한 번도 옮기지 않은 6번째 카드"로 바꾸고, 대조군이 거짓이면 PASS가 되지 않도록 판정식에 넣었다.
  2. 토스트를 못 찾는다며 "8회 연속 실패"가 나왔다. 15%로 8연속은 사실상 불가능해 의심했더니,
     `[role=status]`가 FilterBar 결과 수(`<p role="status">`)를 먼저 잡고 있었다 → `div[role=status]`로 좁힘.
     같은 버그로 성공한 이동을 실패로 보고 다시 눌러 카드가 처우협의까지 갔던 것도 확인했다.
  3. 확장 검증 첫 스크립트에서 토스트를 정리하다 되돌리기 토스트까지 닫았다 → 되돌리기 토스트는 남기고 실패 토스트만 닫도록 분리.
- 브라우저 없이 먼저 자리 규칙만 검증 (scratchpad 스크립트, Node strip-types): 맨 앞·중간·끝 카드 이동 후 되돌리기, anchor 이탈, anchor 없음 10건 통과.
- 직접 확인한 엣지케이스 (헤드리스 Chrome + CDP, 포트 5175/9225, `element.click()`과 `Input.dispatchKeyEvent`/`dispatchMouseEvent` 실제 이벤트):

  | 수용 기준 | 측정값 | 판정 |
  |---|---|---|
  | A 되돌리기 → 원래 순서 | 서류검토 6번째(0-index 5, 420장 중) → 면접 → 되돌리기 후 index 5, 화면·저장 순서 모두 이동 전과 동일. 대조: 이동 중 카드가 컬럼에서 실제로 빠짐 | 충족 |
  | B 새로고침 후 유지 | 새로고침 뒤 화면 순서 = 이동 전 순서, index 5 | 충족 |
  | C 이동 실패 롤백 (리뷰 4) | 3번째 시도에서 실패 발생 → index 5 복귀, 저장 불변, 화면 = 저장. 대조: 낙관적 반영 때 컬럼에서 빠졌음 | 충족 |
  | D 되돌리기 실패 롤백 | 2번째 시도에서 실패 → 면접 index 248/250(끝 아님) 유지, 화면·저장 순서가 되돌리기 직전과 동일, 토스트 "되돌리지 못했습니다. 면접 단계에 그대로 둡니다." | 충족 |
  | E1 키 반복 | `autoRepeat` Ctrl+Z → 토스트 2개·단계 모두 그대로 | 무시됨 |
  | E2 입력창 | 검색창 포커스 중 Ctrl+Z → 되돌리지 않음 | 충족 |
  | E3 한글 입력 | `key='ㅋ'`, `code='KeyZ'` Ctrl+Z → 가장 최근 1건만 되돌림, 앞 토스트 유지 | 충족 |
  | E4 확인 창 | 창이 열린 상태 ⌘+Z → 무시. 대조: 창을 닫은 뒤 ⌘+Z는 되돌림 | 충족 |
  | F0 대조 | 조작 없음 → 4초엔 있고 4,991ms에 사라짐 | 기준선 |
  | F1 포커스 정지 | 되돌리기 버튼 포커스 중 7초 유지 → blur 후 4,992ms에 사라짐 | 충족 |
  | F2 hover 정지 | 마우스 올린 동안 7초 유지 → 벗어난 뒤 4,993ms에 사라짐 | 충족 |
  | G1 재이동 무효화 | 같은 카드 재이동 30ms 뒤(저장값은 아직 면접) 옛 토스트가 사라짐 | 충족 |
  | G2 결과 확정 되돌리기 | "처우협의 → 최종합격" 되돌리기 → 확인 창 없이 처우협의 index 3 복귀, 화면·저장 순서 동일 | 충족 |

- 확인하지 않은 것: 되돌린 뒤 모바일 탭 추종·카드로의 포커스 이동은 범위 밖으로 정해 구현·검증하지 않았다 (DECISIONS.md 가정). 스크린리더 실제 낭독은 확인하지 못했다.
- `npm run typecheck` / `npm run lint`(경고 0) / `npm run build` 통과.

### main 병합 후 fix — keep 롤백 회귀
> main에 병합해 봤더니 rollback-test(#11)의 테스트 1개가 깨진다. 네 브랜치에서 고쳐라. main은 건드리지 마라.
> (실패: `keep 이동이 실패하면 제자리에서 되돌린다` — expected [ "c", "b", "a" ] to deeply equal [ "b", "c", "a" ])
> … 테스트를 고치지 말고 구현을 고쳐라. 그 테스트가 지키는 동작이 사용자가 명시적으로 요청한 것이다 …

- 원인: 리뷰 4번으로 모든 롤백을 `{ after: 같은 컬럼의 앞 카드 }`로 바꾸면서 `'keep'` 이동도 빼냈다가 다시 끼웠다. c 앞에 결과 컬럼 카드가 없어 `after: null` → 배열 맨 앞으로 갔다. 컬럼 안 순서만 보는 내 브라우저 검증(G2 등)으로는 전체 목록 순서 변화를 잡지 못했고, #11의 단위 테스트가 잡았다.
- 수정: `startMove`의 롤백 규칙을 `placement === 'keep'`이면 `'keep'`으로. 테스트는 고치지 않았다.
- 병합: `package-lock.json`의 세션 전 로컬 변경(engines 3줄)이 병합을 막아 diff를 scratchpad에 저장한 뒤 되돌렸다. PROMPTS.md는 공통 지시문 줄에 맞춰 섹션이 엉켜 main 쪽을 기준으로 이 섹션을 뒤에 붙였고, DECISIONS.md는 main 쪽에 이 브랜치의 변경을 다시 적용했다(주요 결정 번호 14 → 16).
- `npm test` 15/15, `tsc -b`, `npm run lint` 통과.

---

## [a11y-keyboard] 키보드 접근성

모델: Claude Opus 5.5 (1M context)

병렬 워크트리(`rpb-11-test`)에서 rollback-test를 끝낸 뒤 이어서 진행했다. 브랜치 `feat/a11y-keyboard`는 사용자 선택에 따라 **아직 main에 병합되지 않은 `feat/detail-panel`(5cba483) 위**에서 시작했다.

### 프롬프트 1
> 너가 제안한 방향대로 가줘

(status에서 제안한 순서였다. 원래 순서는 detail-panel과 loading-error-empty를 먼저, 이어서 병합, 그다음 a11y-keyboard였다. 확인해 보니 앞의 둘은 다른 워크트리에서 이미 진행 중이었다. 그래서 선택지 1~3을 다시 제시했다.)

### 프롬프트 2
> 1-3번중에 고르라는거지?

### 프롬프트 3
> 1번으로 가자

### 프롬프트 4 — 가정·설계 선택 (AI가 제시한 선택지에서 사용자가 선택)
- 베이스 브랜치: **detail-panel 위에서** (대안: main 위에서, drawer 부분 제외)
- `←`/`→`가 동작하는 범위: **카드 안 어느 버튼이든** (대안: 이름 버튼에서만)
- 성공 안내 방식: **보이지 않는 live region** (대안: 성공도 토스트로 / 낙관적 이동 즉시 안내)
- 성공 안내 위치: **Board에서** (대안: useApplicants에서)

### 프롬프트 5 — 리뷰 제안 채택
리뷰 1·2·3번 채택. 4번(aria-keyshortcuts)은 기각했다. 기각 이유를 묻자 사용자는 이렇게 답했다.
> 단축키 안내는 README에 적어주면 좋겠어

### AI 출력 요지
- detail-panel이 카드 `li`에 둔 `onKeyDown`(↑)에 `←`/`→`를 더했다. 경계에서도 `preventDefault`로 가로 스크롤을 막고, 옮길 단계가 없으면 무동작이다.
- 이동은 버튼 클릭과 같은 `onMove` 경로를 탄다. 확인 창, 롤백, 늦은 응답 무시가 그대로 적용된다.
- 카드가 `FocusOrigin`(이름 / 칩 / 이동)을 넘기고, 보드의 `focusSelectors`가 새 위치에서 같은 종류의 버튼을 찾는다. 칩은 결과 컬럼을 떠나면 사라지므로 이름 버튼이 둘째 후보다. 확인 창 취소도 같은 규칙으로 돌아온다.
- 보드가 `confirmed`일 때만 보이지 않는 `role="status"`에 "OOO: 면접 단계로 옮겼습니다."를 넣는다. 번호를 key로 쓰므로 같은 문구도 다시 읽힌다.
- 포커스 표시(`:focus-visible`)는 이름·칩·이동 버튼에 이미 있어 새로 만들지 않았다.

### 리뷰 / 검증
- 리뷰 방식: 동일 세션 자가 리뷰 (5개 관점 체크리스트). `git diff`를 다시 읽고 했다. 결과는 버그·엣지케이스 미통과(키 반복), 구조 미통과(`.srOnly` 중복), 접근성 미통과(단축키가 드러나지 않음), 요구 누락은 브라우저 확인 대기, 미사용 코드 통과(`FocusOrigin` export는 Board 1곳이 사용).
  제안 4건 중 3건 채택, 1건 기각(DECISIONS.md).
- 브라우저 검증 (Claude in Chrome, 포트 5176, 실제 mock API):

  | 수용 기준 | 방법 | 측정값 | 판정 |
  |---|---|---|---|
  | `→` 이동 · 마지막 인덱스 | 이름 버튼 포커스 후 키 입력 | 서류검토 → 면접, index 236 / last 236, 저장값 `interview` | 충족 |
  | 이동 후 포커스 | 스크린샷 | 면접 맨 아래 "안준서" 이름 버튼에 포커스 링 | 충족 (이름 → 이름) |
  | 실제 키 `←` + **실패 롤백** | 실제 키 입력 | 서류검토로 갔다가 15% 실패가 실제로 나 면접 마지막으로 복귀, 저장값 `interview`, 토스트 "이동하지 못해 면접 단계로 되돌렸습니다" | 충족 |
  | 성공 안내 | live region 읽기 | "안준서: 면접 단계로 옮겼습니다." | 충족 |
  | 토글 안내 문구 | 칩 클릭 | "강서연: 최종합격 단계로 변경했습니다." | 채택 2번 반영 확인 |
  | 서류검토 `←` 경계 | 합성 이벤트 | 단계 그대로, `defaultPrevented` true | 무동작 |
  | 결과 `→` 경계 | 합성 이벤트 | `hired` 그대로, `defaultPrevented` true | 무동작 |
  | 키 반복 | `repeat: true` 합성 이벤트 | 단계 그대로 | 채택 1번 반영 확인 |
  | 처우협의 `→` 확인 창 | 실제 키 입력 | 창 열림, 기본 포커스 "취소", 카드는 처우협의 그대로 | 충족 |
  | `ESC` 취소 후 포커스 | 실제 키 입력 | 창 닫힘, 포커스가 **이름 버튼**으로 복귀 (전에는 이동 버튼으로 갔음) | 충족 |
  | `↑` 열기 / `↓` 닫기 | 실제 키 입력 | 열면 포커스가 "상세 닫기", 닫으면 이름 버튼으로 복귀 | 충족 (detail-panel 코드와 통합 확인) |
  | 컬럼 본문 키보드 스크롤 | 실제 Tab 30회 | `scrollTop` 0 → 397px, 포커스 요소가 보이는 영역 안 | `tabindex` 불필요로 판단 |
  | `.srOnly` composes | 클래스 확인 | 안내 영역에 클래스 2개가 합쳐짐 | 채택 3번 반영 확인 |

- **검증 중 막힌 지점**: 처음 보낸 실제 `→` 키가 페이지에 전달되지 않았다. 합성 이벤트로는 이동했지만 포커스가 BODY로 떨어져 있었다.
  원인을 코드로 단정하지 않고 확인했다. 탭이 `visibilityState: hidden`이라 `requestAnimationFrame`이 돌지 않았던 것이다.
  스크린샷으로 한 프레임을 그리게 하자 포커스가 제대로 옮겨져 있었고, 그 뒤로는 실제 키도 전달되었다. 코드 문제가 아니라 검증 환경 문제였다.
- **확인하지 못한 것**: 롤백 뒤의 포커스 복원(`onlyIfFocusLost`)은 탭이 hidden인 동안 일어나 측정하지 못했다. 실제 스크린리더(VoiceOver)로 읽어 보지는 않았고, live region의 DOM 내용만 확인했다.
- `npm run typecheck` / `npm run lint` / `npm run build` 통과. 이 브랜치는 rollback-test 병합 전(detail-panel) 기준이라 `npm test`가 없다.
